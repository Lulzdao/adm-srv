# Скопируйте в .env и заполните реальными значениями. .env в git не коммитить.

PORT=3000
SESSION_SECRET=замените-на-случайную-строку

# --- HTTPS ---
# Шифрование включается само, как только задан сертификат — отдельного
# переключателя нет, чтобы не было состояния «сертификат положили, а включить
# забыли». Ничего не задано — платформа работает по http и предупреждает об
# этом в консоли. Обратного прокси перед приложением нет намеренно: так не
# остаётся параллельного незашифрованного порта, про который легко забыть.
#
# Сертификат ОДИН и общий с «Искрой»: службы стоят на одной машине и отвечают
# на одно имя (p48-srv-adm01.rosstat.local). Подсети две, но в in.local заведена
# запись на этот же адрес и маршрут через шлюз, который его знает, — поэтому
# второго имени и второго сертификата не требуется.
#
# Файл лежит в каталоге certs РАБОТАЮЩЕЙ «Искры»: server.pfx, пароль рядом в
# server.pass (обычный текст, без перевода строки). Обе службы перечитывают его
# сами, без перезапуска, и загрузить новый можно из любой панели — «Сертификаты»
# у платформы или «Сертификат» у «Искры».
#
# ЭТО ЗАПОЛНИТЬ ОБЯЗАТЕЛЬНО, если «Искра» стоит не рядом с платформой. Значение
# по умолчанию (../MESSENGER/certs относительно helpdesk-backend) верно только
# при запуске обеих служб из одного каталога репозитория. На боевом сервере
# «Искра» обычно живёт отдельно:
#   SHARED_CERT_DIR=C:\ISKRA\iskra-server\certs
# Если каталог не найден, платформа поднимется по HTTP и напишет в консоль, где
# именно искала.
SHARED_CERT_DIR=
#
# Вариант 1 — задать сертификат явно, PFX из УЦ домена (экспортировать вместе с
# закрытым ключом и всеми сертификатами пути). Имеет приоритет над хранилищем и
# отключает и слежение за файлом, и загрузку из панели. Осторожно: у «Искры»
# приоритет обратный (сначала хранилище, потом переменные), так что на одной
# машине службы предъявят разные сертификаты. Задавать стоит, только если файла
# в хранилище нет вовсе.
#
# Если такая переменная осталась в окружении Windows от прежних запусков,
# платформа при старте это скажет и подскажет, где её убрать. Отменить её можно
# и отсюда: оставьте строку с пустым значением (TLS_PFX=) — пустое значение в
# .env снимает переменную окружения.
TLS_PFX=
TLS_PFX_PASSWORD=
#
# Вариант 2 — PEM. TLS_CERT обязательно ПОЛНАЯ цепочка
# (cat server.crt chain.crt > fullchain.crt), ключ — без пароля:
TLS_CERT=
TLS_KEY=
#
# --- Доверенные корни (кому доверяет САМА платформа) ---
# Обычно не нужны: удостоверяющий центр один, приложение запускается с
# --use-system-ca и доверяет хранилищу Windows, куда корень приезжает
# групповыми политиками в оба домена.
# Каталог со своими корнями (их видно и правит раздел «Сертификаты» в панели);
# чтобы они действительно применились, каталог указывают в NODE_EXTRA_CA_CERTS
# и перезапускают платформу.
TRUSTED_CA_DIR=
NODE_EXTRA_CA_CERTS=
#
# Важно про модули за HTTPS (сейчас это «Искра»): платформа проверяет их
# сертификат по-настоящему, поэтому в config/modules.js модуль должен быть
# адресован ИМЕНЕМ из его сертификата, а не по 127.0.0.1. Переопределить
# адрес, не трогая код:
MODULE_MESSENGER_URL=

DB_PATH=./data/helpdesk.db
UPLOADS_DIR=./uploads

# --- Группы администраторов ---
#
# DOMAIN_*_ADMIN_GROUP задаёт АДМИНИСТРАТОРОВ ПЛАТФОРМЫ и больше ничего.
# Кто состоит в этой группе — видит все заявки всех отделов, раздел
# администрирования, оповещения, сертификаты и встроенные модули.
#
# Права администратора выдаются ТОЛЬКО отсюда. Через панель администрирования
# их получить нельзя: там настраиваются группы ОТДЕЛОВ-ИСПОЛНИТЕЛЕЙ, включая
# отдел ИТ. Исполнитель ИТ ведёт заявки своего отдела и администратором при
# этом не становится — это разные вещи и разные поля.
#
# Если оставить пусто, администраторов из домена не будет вовсе. Останется
# локальная аварийная учётка (!admin) — она администратор по конфигу
# независимо от домена, см. config/config.js.

# --- Домен А ---
DOMAIN_A_LABEL=Домен А
DOMAIN_A_LDAP_URL=ldaps://dc1.domain-a.local:636
DOMAIN_A_BASE_DN=DC=domain-a,DC=local
# формат: либо UPN (user@domain-a.local), либо через сервисную учётку + поиск по sAMAccountName
DOMAIN_A_SVC_DN=CN=svc-helpdesk,OU=Service Accounts,DC=domain-a,DC=local
DOMAIN_A_SVC_PASSWORD=
DOMAIN_A_ADMIN_GROUP=Lipetskstat-IT

# --- Домен Б ---
DOMAIN_B_LABEL=Домен Б
DOMAIN_B_LDAP_URL=ldaps://dc1.domain-b.local:636
DOMAIN_B_BASE_DN=DC=domain-b,DC=local
DOMAIN_B_SVC_DN=CN=svc-helpdesk,OU=Service Accounts,DC=domain-b,DC=local
DOMAIN_B_SVC_PASSWORD=
DOMAIN_B_ADMIN_GROUP=IT-Otdel-B

# TLS для LDAPS: приложение запускается с флагом --use-system-ca (см.
# package.json), который заставляет Node.js доверять сертификатам из хранилища
# Windows — этого обычно достаточно для внутреннего сертификата AD CS, если
# сервер в домене и корень уже приехал групповыми политиками. Если проверка
# всё равно не проходит (node scripts/check-ldap.js покажет
# UNABLE_TO_VERIFY_LEAF_SIGNATURE) — добавьте корень AD CS в раздел
# «Сертификаты» панели и пропишите NODE_EXTRA_CA_CERTS выше.
#
# Отключение проверки сертификата совсем — только для разового теста,
# никогда не оставлять так на постоянной основе:
LDAP_TLS_REJECT_UNAUTHORIZED=true

# --- Локальный аварийный вход (на случай недоступности обоих доменов) ---
# Логин ОБЯЗАТЕЛЬНО начинается с "!" — по этому символу фронтенд отличает
# локальный вход от доменного, не пытаясь искать такой логин в LDAP.
LOCAL_ADMIN_LOGIN=!admin
LOCAL_ADMIN_PASSWORD_HASH=
# Почта локального администратора. У доменных учёток адрес приезжает из LDAP при
# каждом входе, а у локальных никакого LDAP нет по определению — они для того и
# заведены, чтобы работать, когда домен недоступен. Поэтому адрес задаётся здесь;
# на него уходит пробное письмо из раздела «Оповещения» и оповещения по заявкам,
# созданным под этой учёткой. По умолчанию 48.11@rosstat.gov.ru — значение
# применяется при каждом старте сервера, менять его из интерфейса нельзя.
LOCAL_ADMIN_EMAIL=
# Общая аварийная учётка без прав администратора
LOCAL_USER_LOGIN=!user
LOCAL_USER_PASSWORD_HASH=
# По умолчанию пусто: письма о заявках этой общей учётки никуда не уходят.
# Задайте адрес, если они нужны.
LOCAL_USER_EMAIL=

# --- Автоопределение домена по подсети клиента (вместо вкладок на логине) ---
NETWORK_DOMAIN_A_CIDR=10.148.12.0/22
NETWORK_DOMAIN_B_CIDR=192.168.254.0/23

# Сколько дней сессия остаётся активной без повторного входа (продлевается
# при каждом заходе — "rolling"). Это не настоящий SSO, а длинная сессия.
SESSION_MAX_AGE_DAYS=30

# --- Почта ---
# Настройки почты живут в БАЗЕ и правятся в панели: «Оповещения» -> «Отправка».
# Там же кнопка проверки соединения и пробное письмо — раньше узнать, работает
# ли отправка, можно было только по отсутствию писем.
#
# Значения ниже — запасной путь: они действуют, пока в панели ничего не задали.
# Как только настройки сохранили через веб, они перекрывают этот файл (в панели
# написано, откуда взято действующее значение). Полезно при переносе платформы
# на новое место: письма продолжают уходить со старым .env, пока не дошли руки
# до панели.
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="ИТ-сервисы <helpdesk@lipetskstat.ru>"
