# Искра — десктоп-клиент (Windows)

Приложение для сотрудников: список контактов и отдельные окна чатов, как в Psi, но в современном
виде. Живёт в трее, сообщения приходят и когда окна закрыты. Работает на Windows 7, 8.1 и 10.

Сервер — отдельно, см. [`../iskra-server/`](../iskra-server/README.md). Клиент без него не работает.

## Запуск и сборка

Проверить без сборки установщика:

```bash
npm install
npm start
```

Собрать `.exe` (только на Windows или через GitHub Actions с `windows-latest`):

```bash
npm run dist:win7     # для Windows 7 и 8.1 → dist/win7/iskra-setup-win7-*.exe
npm run dist:win10    # для Windows 10 и новее → dist/win10/iskra-setup-win10-*.exe
npm run dist          # обе сборки подряд
```

**Сборок две, исходник один.** Обе собираются из одного и того же кода — различается только версия
Electron, которую упаковывает установщик: для Windows 7 она заморожена на 22.3.27 (последняя
работающая на этой системе), для Windows 10 берётся современная. Задаётся это в
`build-config/win7.js` и `build-config/win10.js`; отдельных веток под каждую систему нет
намеренно — подробности и обоснование в [CONCEPT-roadmap.md](CONCEPT-roadmap.md).

Установщик для Windows 10 сам откажется ставиться на более старую систему и подскажет, какой файл
взять вместо него. Сборка для Windows 7 работает и на Windows 10 — просто на старом рантайме.

Какая сборка установлена у сотрудника, видно внизу панели настроек в клиенте:
`Искра 1.0.0 · сборка для Windows 7 и 8.1 · Electron 22.3.27`.

## Выпуск обновления

Клиент умеет обновляться сам — сверяет свою версию с файлом на сервере и качает новый установщик.
Порядок выпуска:

1. **Поднять версию** в `package.json` (`npm version patch` — он же поставит git-тег).
   Клиенты сравнивают именно её; без подъёма обновление не увидят.
2. **Собрать обе сборки**: `npm run dist`.

   В каждой папке появится по два файла — установщик и `latest.yml`. Второй создаёт сам
   `electron-builder` (в нём версия, имя файла и контрольная сумма); руками его писать не нужно
   и нельзя — контрольная сумма должна совпадать с реальным файлом, иначе клиент откажется
   ставить скачанное.

   Установщик **один на сборку и содержит обе разрядности сразу** — отдельных файлов под 32 и 64
   бита нет намеренно (почему — в комментарии к `artifactName` в `build-config/win7.js`).

3. **Выложить на сервер** содержимое каждой папки в свою — рядом с `server.js`:

   ```
   iskra-server/updates/win7/    ← iskra-setup-win7-<версия>.exe  + latest.yml
   iskra-server/updates/win10/   ← iskra-setup-win10-<версия>.exe + latest.yml
   ```

   Оба файла обязательны и должны лежать вместе: `latest.yml` — это то, что клиент читает при
   проверке, а `.exe` — то, что он потом качает.

   Папки раскладывать **строго раздельно**: сборка для Windows 10 несёт Electron, который на
   Windows 7 не запускается, и клиент, скачавший чужое обновление, перестанет открываться.
   Перезапускать сервер не нужно — файлы отдаются как есть.

Дальше клиенты подхватят обновление сами: проверка идёт при запуске приложения и по кнопке
«Проверить сейчас» в настройках. Скачанное **устанавливается при обычном выходе** из приложения —
принудительных перезапусков нет; кто хочет сразу, нажимает «Перезапустить и обновить».

Адрес, откуда качать, клиент вычисляет от того сервера, к которому реально подключён (включая
адрес, заданный по Ctrl+S) — при переезде сервера обновления переезжают вместе с ним, пересобирать
установщики ради этого не нужно.

## Работа с HTTPS и сертификатом домена

Внутри клиента **два независимых сетевых стека**, и это не мелочь.

Окна (чат, список контактов, объявления) работают через Chromium — он читает хранилище
сертификатов Windows, куда корневой сертификат домена уже роздан групповыми политиками. Здесь
делать нечего.

Главный процесс — автообновление и отправка файлов правым кликом по контакту — работает через
Node, а он хранилище Windows **не читает**, групповые политики на него не действуют вообще.
Без отдельной правки после перехода сервера на https получилась бы очень запутанная картина:
переписка работает, а обновления и отправка файлов молча отваливаются.

Поэтому в сборку вшит `rosstat-root-ca.crt` — публичный корневой сертификат домена
(`rosstat-CA-SRV-CA01-CA`, действует до января 2036). Приватного ключа в нём нет, это просто якорь
доверия, который и так есть на всех машинах домена, — держать его в репозитории безопасно.
Подключает его `trustOrganizationCa()` в начале `main.js`.

Важно: там именно **добавляется** доверие к одному конкретному центру, а не отключается проверка —
144 публичных корня остаются на месте, сертификат с чужим именем по-прежнему отвергается. Не
поддавайтесь соблазну «починить» TLS обработчиком `certificate-error` с игнорированием ошибки: это
уничтожит защиту полностью — соединение останется шифрованным, но подменить сервер сможет любой в
сети, и выглядеть всё будет исправно.

Если удостоверяющий центр организации когда-нибудь сменится — заменить файл и пересобрать.
Заметить это можно не по жалобам сотрудников: сервер сравнивает свой корневой сертификат с этим
файлом и пишет в журнал `tls_root_differs_from_client`, а в панели, в разделе «Сертификат»,
показывает это прямым предупреждением.

## Видно ли, что связь шифруется

По внешнему виду приложения этого не понять, а разница принципиальная: по `http` пароль и вся
переписка идут открытым текстом. Поэтому состояние соединения показано в трёх местах:

- **на экране входа** — строка под формой: «Соединение защищено» серым или «Соединение без
  шифрования» красным. Именно здесь уходит пароль, поэтому и надпись здесь;
- **в строке профиля** — красная метка «без шифрования» рядом с именем. Когда всё в порядке, метки
  нет вовсе: постоянный зелёный замочек через неделю перестают замечать, а нужно заметить именно
  неисправность;
- **в настройках**, раздел «Соединение с сервером» — полный адрес, состояние и, если адрес
  переопределён вручную на этой машине, какой адрес был в сборке.

Определяется это по адресу сервера (`https://` или `http://`) — то есть по тому, как соединение
устанавливается на самом деле, а не по настройке где-то в стороне.

## Смена адреса сервера после входа: Ctrl+Shift+S

На экране входа адрес меняется по **Ctrl+S** (см. ниже). Того же самого иногда нужно уже после
входа — например, чтобы проверить, поднялся ли https на сервере, не выходя из аккаунта. Для этого:
открыть панель настроек и нажать **Ctrl+Shift+S**.

Откроется служебное окно с адресом и двумя кнопками, `http` и `https`, — они правят только начало
строки, сохраняется всегда адрес целиком. После сохранения приложение перезапускается: адрес читают
все три окна независимо, каждое в свой момент, и переподключение «на лету» оставило бы половину
состояния от старого адреса.

**Кнопка «Проверить» — главное в этом окне.** Она обращается к `/api/ping` по введённому адресу и
говорит одно из трёх: сервер отвечает и соединение шифруется; сервер отвечает, но **без**
шифрования; связаться не удалось. Без неё смена адреса была бы прыжком вслепую — приложение
перезапускается, и только там выясняется, что по новому адресу никто не отвечает (чинить это
пришлось бы уже с экрана входа по Ctrl+S).

Ради неё же в окне и кнопки протокола: сами по себе они ничего не включают — сервер работает либо
по http, либо по https, и переключателя «здесь» у него нет. Их смысл в проверке: переключил
протокол, нажал «Проверить» и увидел, отвечает ли сервер именно так. Пригождается это в двух
случаях — когда сервер перевели на https, а установщики собраны со старым адресом (пересобирать
ради этого не нужно), и когда надо на месте понять, почему у человека не подключается.

Комбинация выбрана не `Ctrl+A` намеренно: в Windows это «выделить всё», и перехватывать её значило
бы сломать привычное поведение в полях ввода. `Ctrl+Shift+S` рифмуется с `Ctrl+S` на экране входа —
то же действие, тот же смысл.

**Адрес сервера** по умолчанию зашит в `config.js` (`SERVER_URL`) — меняйте там и пересобирайте
установщик. Он обязан **дословно совпадать** с именем в поле SAN сертификата сервера: сейчас это
`https://p48-srv-adm01.rosstat.local:3103`. По IP-адресу (`10.148.130.210`) подключиться нельзя —
имени в сертификате нет, и клиент отвергнет соединение (`ERR_TLS_CERT_ALTNAME_INVALID`). Что именно
записано в сертификате, сервер пишет в свой лог при запуске строкой `tls_certificate`. Пересборка ради смены адреса нужна не всегда: на экране входа по **Ctrl+S** открывается
скрытое поле ввода адреса, оно сохраняется в настройках этого компьютера и имеет приоритет над
`config.js`. Поле специально не на виду — это не то, что должен трогать рядовой сотрудник.

**Иконки.** `build/icon.ico` — приложение (ярлык, панель задач), `tray-icon.ico` — трей. Оба
`.ico`, оба многослойные (несколько размеров в одном файле — 16/24/32/48/64/128/256 px), иначе
Windows возьмёт первый попавшийся размер и замылит иконку. `build/icon.png` рядом лежит для
превью/документации — сама сборка его не использует (в коде и в `files` секции ссылки на него нет,
работает только `.ico`).

`tray-icon-fallback.png` — на случай, если `.ico` не декодируется: `nativeImage.createFromPath`
грузит `.ico` через декодер операционной системы, и на Windows (для чего собственно приложение и
собирается) это штатно, но на Linux он молча отдаёт пустое изображение без единого предупреждения —
проверено вживую. Без запасного PNG трей в этом случае остался бы вовсе без иконки. `createTray()` в
`main.js` сначала пробует `.ico`, и только если он оказался пустым — берёт PNG.

## Про Windows 7

Electron 23 и новее отказался от Windows 7 и 8.1, поэтому сборка win7 заморожена на
**electron 22.3.27** — последней работающей версии (`build-config/win7.js`). Поднимать её нельзя,
пока такие машины в парке; для новых систем есть отдельная сборка win10, см. раздел о сборке выше.

**Отсюда правило для кода: он должен работать на Electron 22.** В `devDependencies` стоит именно
эта версия, то есть `npm start` при разработке запускает её — несовместимость всплывёт сразу.
Понадобился API, которого в 22 нет? Не форк, а проверка наличия:
`if (someApi) { ... } else { запасной путь }`.

**Ускорение графики на Win7 отключено намеренно** (`app.disableHardwareAcceleration()` в начале
`main.js`, только при версии ядра «6.1»). Драйверы DirectX тех лет не рассчитаны на современный
Chromium и роняли приложение. На более новых Windows ускорение остаётся включённым — теряется
только плавность анимаций.

**Про краш `0x80000003` при выключении компьютера.** Он был вызван тем, что обработчик закрытия
окна списка контактов в обычной ситуации отменяет закрытие и прячет окно в трей — а во время
настоящего завершения сеанса Windows такой отказ ломает штатное завершение Chromium. Подробный
разбор всех трёх причин и того, как это устроено сейчас, — в комментарии к `beginShutdown()` в
`main.js`. Если будете трогать этот код, прочитайте комментарий целиком: там же объяснено, почему
уборку нельзя выполнять прямо внутри перехватчика оконных сообщений.

## Возможности

**Список контактов.** Люди сгруппированы по отделам, у каждого отдела счётчик «в сети/всего», отдел
сворачивается кликом. Сотрудник может состоять сразу в нескольких отделах — тогда он показан в
каждом из них, и это не сбой отображения. Чтобы найти человека, просто начните печатать имя — сверху
появится строка поиска. Escape возвращает обычный вид. Двойной клик открывает чат, правый клик —
быстрое меню «Написать» / «Отправить файл».

**Сообщение всему отделу.** Правый клик по названию отдела → «Сообщение всему отделу». Откроется то
же окно, что и у объявлений — с файлами, перетаскиванием и поиском по дням, — но получат сообщение
только сотрудники этого отдела. Право «Рассылки» для этого не нужно: оно про объявление всей
организации. У получателей сообщение приходит уведомлением и попадает в «Объявления», где помечено
названием отдела. Ленту отдела видят только его сотрудники: постороннему окно остаётся формой
отправки.

**Личные чаты, общая комната и группы.** Группу может собрать любой сотрудник под свою задачу:
кнопка «+» в разделе «Группы». Переименовать её, менять состав и удалить может создатель или
администратор; выйти самому можно всегда.

**Чат очищается каждый день — но ничего не теряется.** При открытии окна показаны только сегодняшние
сообщения. Кнопка-часы «История» в шапке открывает панель: месяцы → дни с количеством сообщений, и
полнотекстовый поиск по всей переписке с этим человеком или группой. Внутри дня старые сообщения
подгружаются по мере прокрутки вверх. Дни считаются по часовому поясу вашего компьютера, даже если
сервер стоит в другом.

**Ответы и реакции.** Наведите курсор на сообщение — над ним появится панелька: ответить или
поставить смайлик. Цитата в ответе сохраняется навсегда, даже если оригинал давно уехал вверх.

**«Печатает».** Пока собеседник набирает текст, внизу переписки появляется пузырёк с бегущими
точками.

**Отметки о прочтении.** У своих сообщений в личной переписке — галочка: серая, пока собеседник не
прочитал, синяя после. В группах и общей комнате галочек нет: там «прочитано» ни о чём не говорит.

**Непрочитанные.** На контактах и на «Объявлениях» — счётчик. Он переживает перезапуск клиента:
пропущенное, пока приложение было закрыто, сервер досчитывает при следующем входе.

**Объявления (рассылки).** Пункт в самом верху списка контактов, виден всем. У большинства это
просто лента объявлений; у кого есть право «Рассылки» — в шапке появляется кнопка «Написать».
К объявлению можно приложить файлы. Сюда же попадают сообщения отделам, в которых человек состоит —
они помечены названием отдела, чтобы было видно, что это пришло не всей организации.

**Файлы.** Скрепка в чате, правый клик по контакту → «Отправить файл…» (работает, даже если чат
не открыт), либо перетаскивание прямо в окно. Можно выбрать сразу несколько — уйдут одним
сообщением, с подписью или без. Во время скачивания иконка превращается в кольцо с процентами, по
завершении — в зелёную галочку. Папку для сохранения можно задать в настройках, тогда файлы будут
складываться туда без вопросов. Максимальный размер и разрешённые типы задаёт администратор в
веб-панели сервера.

**Статусы.** Зелёный — за компьютером, жёлтый — отошёл, серый — мессенджер не запущен. Учитывается
реальная активность мыши и клавиатуры во всей системе, а не только в мессенджере. Через сколько
минут включается «Отошёл» — настраивается, по умолчанию 30. При наведении видно, как давно человек
отошёл, а если он работает с нескольких компьютеров — имена всех.

> На виртуальной машине статус «Отошёл» может не наступать: интеграция мыши между хостом и гостем
> сама генерирует движения курсора, и Windows считает это активностью. Это особенность ВМ, а не
> ошибка клиента — на обычном компьютере всё определяется правильно.

**Профиль и настройки** — клик по своему имени вверху, панель раскрывается прямо под ним:
отображаемое имя, тема (тёмная/светлая, применяется сразу во всех окнах), масштаб интерфейса,
открывать ли окно чата при новом сообщении, порог «Отошёл», запоминать размеры окон, держать окна
поверх остальных, папка для файлов, выход из аккаунта.

**Подсказка про Caps Lock** на экране входа — пароль не виден, и набранный с включённым Caps Lock он
выглядит точно так же, как правильный.

**Фоновая работа.** Закрытие окна списка контактов прячет приложение в трей, а не выключает его.
Полный выход — пункт «Выход» в меню трея.

**Администрирование только через веб-панель сервера.** В клиенте его нет намеренно: раньше было две
почти одинаковые панели, и они разъезжались между собой.

## Если будете дорабатывать

**Общие файлы окон.** `renderer/theme.css` (переменные тем, скроллбары, модальные окна) и
`renderer/ui-kit.js` (SVG-иконки, `uiAlert`/`uiConfirm`, применение темы, отправка ошибок на сервер)
подключены на каждой странице. Добавляете новое окно — подключите оба.

**Экранируйте всё, что пришло от людей.** Имя сотрудника он задаёт себе сам, и без `escapeHtml()`
оно попадает в разметку как код. Это уже приводило к настоящей уязвимости — см. историю коммитов.
`escapeHtml` экранирует и кавычки, поэтому годится и для значений внутри атрибутов.

**Эмодзи рисуются картинками, а не шрифтом.** На Windows 7 нет цветного эмодзи-шрифта. Текст
сообщений прогоняется через `window.emojiHtml()` (`ui-kit.js`), картинки лежат в `renderer/emoji/`
(графика Google Noto Emoji, лицензия MIT/Apache 2.0; эмодзи Apple использовать нельзя — они
проприетарные). Картинки есть **только** для набора из панели выбора (`EMOJI_LIST` в `chat.html`,
около 50 штук) — добавляете новый смайлик в список, положите рядом и его `.png` (имя файла — hex-код
символа, например `1f600.png`). Любой другой эмодзи просто останется текстовым символом. Отдельно
подключён чёрно-белый шрифт Noto Emoji (`renderer/fonts/`) — чтобы вместо пустых квадратов на Win7
было видно хоть что-то.

**Каждое окно чата держит своё соединение с сервером** и обязано само сообщать по нему статус
бездействия. Иначе сервер считает это соединение вечно активным, и статус «Отошёл» не наступает,
пока открыт хоть один чат. Делаете новое окно со своим соединением — не забудьте про это.

**Не смешивайте `-webkit-app-region: no-drag` с `transition` на одном элементе.** Такое сочетание
в Chromium вызывает пересчёт зоны перетаскивания прямо во время анимации, и элемент начинает мигать
при наведении по 8–10 раз в секунду. `no-drag` нужен только тому, что реально лежит внутри шапки окна.

**Не пересобирайте весь список на каждое сообщение о присутствии.** Оно приходит при смене статуса
любого сотрудника; если в этот момент пересоздать строки, элемент под курсором потеряет наведение и
список будет моргать. Для этого есть отдельная лёгкая `updatePresenceOnly()`; полная пересборка —
только когда действительно меняется состав списка.

**`-webkit-app-region: drag` не наследуется.** Добавили текст в шапку и хотите таскать окно за него
— поставьте `drag` на сам элемент, а не только на родителя.

**Файлы сообщения** хранятся в колонке `files_json` таблицы `messages` (массив `{url, name, size}`)
— одно сообщение несёт сколько угодно файлов. Старые колонки `file_url`/`file_name`/`file_size`
остались только ради сообщений, отправленных до этого изменения; и то и другое приводит к общему
виду `normalizeRow()` в `server.js`. Правите выборки из `messages` — помните, что почти все они
проходят через неё.

## Что дальше

- Автозапуск при входе в Windows и автообновление (`nsis.runAfterFinish`, `electron-updater`);
- разделение сборок под Win7 и Win10 — см. [`CONCEPT-roadmap.md`](CONCEPT-roadmap.md).
