// ============================================================================
//  НАСТРОЙКИ ПО УМОЛЧАНИЮ ДЛЯ СБОРКИ КЛИЕНТА
//  Значение отсюда используется, пока никто не переопределил адрес вручную (см. ниже) — при
//  массовой раздаче новым сотрудникам меняйте здесь и пересобирайте установщик.
//  Ручной override без пересборки: на экране входа Ctrl+S открывает скрытое поле ввода адреса
//  (см. roster.html) — сохраняется в settings.json (serverUrlOverride) и имеет приоритет над
//  этим файлом. Специально не на виду — это не то, что должен трогать рядовой сотрудник.
//
//  Адрес обязан ДОСЛОВНО совпадать с именем в поле SAN сертификата сервера — иначе клиент
//  отвергнет соединение по несовпадению имени. В сертификате домена записано ровно
//  DNS:p48-srv-adm01.rosstat.local, IP-адреса в нём нет: по 10.148.130.210 подключиться не выйдет,
//  проверено вживую (ERR_TLS_CERT_ALTNAME_INVALID). Отсюда же требование к DNS: имя должно
//  разрешаться на всех рабочих местах.
// ============================================================================
module.exports = {
  SERVER_URL: 'https://p48-srv-adm01.rosstat.local:3103',
};
