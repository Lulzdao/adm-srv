export const meta = {
  name: 'adm-srv-audit-deep',
  description: 'Полный аудит adm-srv: архитектура, дефекты, дублирование, производительность, качество тестов — с адверсариальной проверкой каждой находки',
  phases: [
    { title: 'Карта', detail: 'три независимых чтения репозитория и сведение в одну карту' },
    { title: 'Поиск', detail: 'находки по двенадцати направлениям' },
    { title: 'Проверка', detail: 'скептик опровергает находки своего участка' },
    { title: 'Экспертиза', detail: 'три независимых взгляда на каждую серьёзную находку' },
    { title: 'Полнота', detail: 'что осталось непосмотренным' },
  ],
}

// ---------------------------------------------------------------------------
//  Общий контекст. Без него агенты предлагают React, npm-пакеты и :has() —
//  всё то, что на закрытом контуре Windows Server 2016 просто не поедет.
// ---------------------------------------------------------------------------
const CONTEXT = `
ПРОЕКТ: /home/user/adm-srv — внутренняя ИТ-платформа Липецкстата (территориальный
орган Росстата). Изолированная сеть без интернета, Windows Server 2016, Node.js 22.
Организация ~100-200 сотрудников, несколько заявок в день, тысячи звонков в месяц.

Состав (актуальные размеры):
  helpdesk-backend/  — платформа: заявки, вход через LDAP, прокси модулей, оповещения
                       server.js 85, routes/tickets.js 579, routes/notifications.js 393,
                       routes/certificates.js 248, routes/auth.js 169, routes/modules.js 110,
                       routes/admin.js 82, routes/departments.js,
                       services/tls.js 366, services/notifications.js 256,
                       services/mailer.js 214, services/ldapAuth.js 161,
                       services/scheduler.js 148, services/sources/certs.js 187,
                       services/sources/smdr.js 66, services/moduleClient.js 57,
                       services/userStore.js 46, services/settings.js, services/network.js,
                       config/config.js 118, config/notifications.js 176, config/modules.js 60,
                       config/departments.js 20, middleware/auth.js 20,
                       db/schema.sql 161, db/init.js 135,
                       public/app.js 2034, public/styles.css 473, public/index.html
  CERTVIEWER/        — модуль: реестр сертификатов и МЧД (server.js 466, mchd.js 214, три страницы)
  SMDR/              — модуль: журнал звонков (web-node/server.js 197 на Express+EJS,
                       web-node/db.js 20, Collector.Py ~250 — сборщик с АТС)
  MESSENGER/         — мессенджер «Искра»: server.js 2318, public/index.html 1744,
                       desktop-client/ на Electron

ЖЁСТКИЕ ОГРАНИЧЕНИЯ ПРОЕКТА — предложения, которые их нарушают, бесполезны:
  1. НЕТ шага сборки. Фронтенд — чистые HTML/CSS/JS, никаких бандлеров и фреймворков.
     React рассматривали и осознанно отвергли. Не предлагай его снова.
  2. НЕТ новых npm-зависимостей без крайней нужды: сеть изолирована, нативные
     модули (node-gyp) не соберутся. Поэтому в платформе встроенный node:sqlite
     (DatabaseSync — СИНХРОННЫЙ API), встроенный node:test, встроенный zlib,
     разбор ZIP руками.
  3. Браузеры на рабочих машинах — Chromium 109. Никакого oklch, :has(),
     новых DOM API. Веб-шрифтов нет — интернета нет.
  4. MESSENGER/ — ВЕНДОРЕННАЯ КОПИЯ чужого репозитория Lulzdao/Iskra-messenger,
     её периодически сливают с upstream. НЕ предлагай там рефакторинг структуры:
     это создаст вечный конфликт слияния. Отмечать можно только дефекты
     безопасности и дефекты в наших собственных правках.
  5. Комментарии в коде на русском и объясняют ПОЧЕМУ, а не ЧТО. Это принятый
     стиль, сохраняй его в предлагаемом коде. Не предлагай их сокращать.
  6. Осознанные решения, которые НЕ являются недоделками:
     - из архива МЧД сохраняются ТОЛЬКО ФИО, реестровый номер и срок;
       паспорт/СНИЛС/ИНН не хранятся НАМЕРЕННО;
     - логин и пароль АТС лежат внутри SMDR/Collector.Py, а не в .env — решение владельца;
     - модули (CERTVIEWER, SMDR) доверяют запросам за платформой: слушают только
       127.0.0.1, CERTVIEWER при BEHIND_GATEWAY=true пропускает без проверки.

ЧТО ПОЯВИЛОСЬ НЕДАВНО и в прошлый аудит не попадало:
  - целый раздел оповещений: config/notifications.js (справочник категорий),
    services/notifications.js (модель «событие отдельно от доставки»),
    services/mailer.js (SMTP настраивается из панели, настройки в БД),
    services/scheduler.js (setInterval раз в час + отметка «сделано» в settings),
    services/sources/certs.js и sources/smdr.js (адаптеры к модулям),
    services/moduleClient.js (платформа ТЯНЕТ данные из модулей по HTTP),
    routes/notifications.js, три вкладки во фронтенде;
  - таблицы notification_events / notification_deliveries / notification_settings;
  - локальные аварийные учётки со статичным адресом почты;
  - в MESSENGER — наши правки в presenceSnapshot() и в разметке списка пользователей.

ТЕСТЫ УЖЕ ЕСТЬ — 79 штук, все зелёные, на встроенном node:test:
  helpdesk-backend/test/notifications.test.js  16
  helpdesk-backend/test/expiry.test.js         16
  helpdesk-backend/test/scheduler.test.js      14
  helpdesk-backend/test/localAccounts.test.js   7
  helpdesk-backend/test/helpers/{tempDb,fakeSmtp,fakeModules}.js — оснастка
  CERTVIEWER/test/mchd.test.js                 23
  CERTVIEWER/test/zipFixture.js — сборка ZIP руками, включая испорченные
Запуск: npm test в соответствующем каталоге (именно "node --test" без аргумента).
НЕ покрыты совсем: routes/tickets.js, routes/auth.js, routes/certificates.js,
services/tls.js, services/ldapAuth.js, весь SMDR, весь фронтенд.

ЗАПРЕТ НА ДАННЫЕ: в проекте есть архивы с персональными данными. НИКОГДА не
приводи в ответе настоящие ФИО, СНИЛС, ИНН, паспорта, реестровые номера,
адреса почты сотрудников из баз и файлов данных. Все примеры — выдуманные.
Файлы helpdesk-backend/data/*.db читать не нужно.
`

const FINDING_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['area', 'summary', 'findings'],
  properties: {
    area: { type: 'string' },
    summary: { type: 'string', description: 'Честная оценка состояния участка, 3-5 предложений: что хорошо, что болит' },
    findings: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['id', 'title', 'severity', 'category', 'file', 'line', 'now', 'why_bad', 'fix', 'failure_scenario', 'effort'],
        properties: {
          id: { type: 'string', description: 'краткий слуг, напр. tickets-attach-path' },
          title: { type: 'string' },
          severity: { type: 'string', enum: ['critical', 'high', 'medium', 'low'] },
          category: {
            type: 'string',
            enum: ['bug', 'security', 'architecture', 'duplication', 'naming', 'dead-code',
                   'error-handling', 'performance', 'hardcode', 'complexity', 'leaky-abstraction', 'test-gap'],
          },
          file: { type: 'string' },
          line: { type: 'integer' },
          now: { type: 'string', description: 'Что сейчас — кратко, с точным фрагментом кода' },
          why_bad: { type: 'string', description: 'Почему это плохо — конкретное следствие, не «нарушает принципы»' },
          fix: { type: 'string', description: 'Как переписать — ГОТОВЫЙ НОВЫЙ КОД целиком, с русскими комментариями про ПОЧЕМУ' },
          failure_scenario: { type: 'string', description: 'Для bug/security/performance: конкретные входные данные -> неверный результат. Иначе пустая строка' },
          effort: { type: 'string', enum: ['минуты', 'часы', 'день+'] },
        },
      },
    },
  },
}

const VERDICT_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['verdicts'],
  properties: {
    verdicts: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['id', 'real', 'reason', 'fix_ok'],
        properties: {
          id: { type: 'string' },
          real: { type: 'boolean', description: 'true — подтверждено чтением кода; false — опровергнуто' },
          severity_corrected: { type: 'string', enum: ['critical', 'high', 'medium', 'low', ''] },
          reason: { type: 'string', description: 'Что именно проверил и что увидел в коде' },
          fix_ok: { type: 'boolean' },
          fix_problem: { type: 'string' },
        },
      },
    },
  },
}

const LENS_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['refuted', 'note'],
  properties: {
    refuted: { type: 'boolean', description: 'true — находка НЕ подтвердилась' },
    note: { type: 'string', description: 'Что проверил и что увидел. Со ссылкой на файл:строку' },
    severity_corrected: { type: 'string', enum: ['critical', 'high', 'medium', 'low', ''] },
    fix_ok: { type: 'boolean' },
  },
}

// ---------------------------------------------------------------------------
//  Карта архитектуры. Три независимых чтения, потом сведение: один агент на
//  20 тысяч строк неизбежно смотрит по верхам.
// ---------------------------------------------------------------------------
const MAP_READERS = [
  {
    key: 'ядро',
    prompt: `Прочитай ядро платформы: helpdesk-backend/server.js, middleware/auth.js,
routes/*.js (все семь), config/config.js, config/departments.js, config/modules.js,
db/schema.sql, db/init.js.
Опиши: точки входа HTTP, где проходит проверка прав, кто с кем говорит,
какие слои и где они протекают (SQL в обработчике, разметка в сервисе и т.п.),
где живёт состояние.`,
  },
  {
    key: 'сервисы',
    prompt: `Прочитай слой сервисов: helpdesk-backend/services/*.js и services/sources/*.js,
config/notifications.js. Это новая и самая сложная часть проекта.
Опиши: модель оповещений (событие / доставка / настройки), как платформа тянет
данные из модулей, как устроен планировщик, где границы ответственности между
сервисами и где они размыты, какие есть неявные связи через таблицу settings.`,
  },
  {
    key: 'модули-и-фронт',
    prompt: `Прочитай границы модулей и фронтенды: CERTVIEWER/server.js, CERTVIEWER/mchd.js,
SMDR/web-node/server.js, SMDR/web-node/db.js, SMDR/Collector.Py,
helpdesk-backend/public/app.js (2034 строки — читай целиком, по частям),
helpdesk-backend/public/index.html, CERTVIEWER/public/*.html.
MESSENGER/server.js — только бегло, чтобы понять границу.
Опиши: где проходят доверительные границы между процессами, как платформа
проксирует модули, как устроен фронтенд платформы (роутинг, перерисовка,
состояние), что дублируется между четырьмя фронтендами.`,
  },
]

const AREAS = [
  {
    key: 'tickets',
    label: 'заявки',
    prompt: `Разбери helpdesk-backend/routes/tickets.js (579 строк) — самый большой маршрут проекта,
вместе с middleware/auth.js, config/departments.js, routes/departments.js.

Ищи: слишком длинные обработчики, повторяющуюся валидацию, SQL прямо в обработчике,
дублирование правил доступа, магические константы, проглоченные ошибки, N+1 запросы,
небезопасную работу с путями при загрузке вложений, неполную валидацию входа,
рассинхрон между списком статусов на сервере и во фронтенде.

Особое внимание: canAccessTicket, getTicketDetail, nextDisplayId, ticketPayload,
deptUserIds, обработка вложений (multer, имя файла на диске, отдача файла обратно),
как считаются права ролей it / исполнитель / автор, и что происходит при удалении заявки
с вложениями и оповещениями (ON DELETE CASCADE).`,
  },
  {
    key: 'frontend',
    label: 'фронтенд платформы',
    prompt: `Разбери helpdesk-backend/public/app.js (2034 строки), public/index.html, public/styles.css (473).
Это единый файл-модуль без сборки — «god-модуль» по определению; вопрос в том, где он реально болит.

Ищи: функции длиннее ~80 строк, повторяющиеся куски разметки, ПРОПУЩЕННОЕ ЭКРАНИРОВАНИЕ
(проверь КАЖДУЮ подстановку в innerHTML — есть ли места, где esc() забыли; это была бы XSS),
полную перерисовку innerHTML там, где теряется состояние (фокус, прокрутка, ввод),
дублирование констант между фронтендом и сервером (статусы, приоритеты, роли, цвета),
мёртвый код, обработчики без снятия и утечки при перерисовке, необработанные отказы fetch,
опрос по таймеру (что и как часто тянет).

Проверь недавние правки: сохранение вкладки через history/hashchange, восстановление
прокрутки меню, выпадающие списки шаблонов и получателей.
НЕ предлагай React/Preact/бандлеры.`,
  },
  {
    key: 'notif-service',
    label: 'оповещения: сервисный слой',
    prompt: `Разбери сердце новой подсистемы: helpdesk-backend/services/notifications.js (256),
services/mailer.js (214), config/notifications.js (176), db/schema.sql (таблицы notification_*),
db/init.js (migrateNotifications).

Ищи: гонки (emit пишет в БД синхронно, отправка асинхронна), потерю доставок,
неверную работу dedup_key, что будет при параллельных запросах, ошибки в SQL,
транзакционность (пишутся ли событие и доставки атомарно), утечку соединений SMTP,
поведение при пустых настройках, при удалении пользователя, при выключенной категории.

Особое внимание:
  - emit(): что если категория отключена, получателей нет, dedup сработал — что возвращается
    и правильно ли это обрабатывают вызывающие;
  - resolveEmails(): три режима (список / заимствование / автор) — нет ли зацикливания
    при взаимном заимствовании категорий;
  - render(): подстановка {{кириллица}} — что при отсутствующей переменной, при вложенных скобках;
  - retryPending / backfillDeliveries: границы, лимиты, что при 10 000 висящих доставок;
  - mailer: хранение пароля SMTP в БД открытым текстом — оценить риск и предложить решение,
    выполнимое без новых зависимостей.`,
  },
  {
    key: 'notif-sched',
    label: 'оповещения: планировщик и источники',
    prompt: `Разбери helpdesk-backend/services/scheduler.js (148), services/sources/certs.js (187),
services/sources/smdr.js (66), services/moduleClient.js (57), services/settings.js.

Ищи прежде всего дефекты арифметики дат и логики окон:
  - что если tick() выполняется дольше часа и перекроется сам с собой;
  - переход через полночь, конец месяца, високосный год, смена часового пояса,
    переход на летнее время (в РФ его нет, но сервер может стоять в другой зоне);
  - daysLeft(): границы 0 / -1 / null, некорректная дата, дата с временем;
  - skipExpired() и EXPIRED_GRACE_DAYS: правильно ли отсекается архив;
  - previousMonth(): границы 1 января, 1 марта;
  - что если модуль вернул 10 000 документов; что если вернул один и тот же ref дважды;
  - windowKey для monthly: если сервер простоял весь месяц, отправится ли отчёт за пропущенный.

Проверь также, что произойдёт при первом запуске на чистой базе и при запуске
на базе, где оповещения уже год как работают.`,
  },
  {
    key: 'routes-auth',
    label: 'вход, права и маршруты',
    prompt: `Разбери helpdesk-backend/routes/auth.js (169), routes/admin.js (82),
routes/modules.js (110), routes/notifications.js (393), middleware/auth.js,
services/ldapAuth.js (161), services/userStore.js (46), config/config.js (118), server.js (85).

Ищи прежде всего ДЕФЕКТЫ БЕЗОПАСНОСТИ И ЛОГИКИ:
  - права: пройди по КАЖДОМУ маршруту и проверь, стоит ли requireAuth/requireRole;
    выпиши маршруты без проверки отдельно;
  - подстановка пользовательского ввода в SQL, в пути файлов, в ключи settings,
    в LDAP-фильтр (инъекция в LDAP-фильтр — реальный класс уязвимости);
  - ограничение попыток входа: обходится ли (сброс счётчика, гонка, хранение в памяти,
    перезапуск сервера, разный регистр логина);
  - что отдаётся наружу: не утекают ли хэши паролей, пароль SMTP, пути, содержимое ошибок LDAP;
  - сессии: секрет по умолчанию, флаги cookie, срок, CSRF;
  - прокси модулей (routes/modules.js): можно ли через него достучаться туда, куда не следует
    (подстановка пути, обход префикса, доступ к модулю без прав);
  - обработка ошибок: везде ли JSON, не утекает ли стек.

Для КАЖДОЙ находки категории security обязателен failure_scenario:
конкретный HTTP-запрос -> конкретное последствие.`,
  },
  {
    key: 'tls',
    label: 'сертификаты сервера и TLS',
    prompt: `Разбери helpdesk-backend/services/tls.js (366 — самый большой сервис, не покрыт тестами вовсе)
и helpdesk-backend/routes/certificates.js (248).

Это код, который управляет сертификатами и закрытыми ключами. Ищи:
  - работу с путями из пользовательского ввода (обход каталога при загрузке/удалении);
  - права на файлы закрытых ключей, попадание ключей и паролей в логи и в ответы API;
  - разбор PFX/PEM: что при битом файле, при неверном пароле, при огромном файле;
  - поведение при отсутствии файла, при недоступном хранилище, при гонке
    (перезапись сертификата во время чтения);
  - обработку ошибок: что увидит пользователь, что попадёт в консоль;
  - god-модуль: какие обязанности смешаны и как их разделить;
  - валидацию загрузки: размер, тип, имя.

Для каждой находки security обязателен failure_scenario.`,
  },
  {
    key: 'certviewer',
    label: 'Сертвивер',
    prompt: `Разбери модуль CERTVIEWER целиком: server.js (466), mchd.js (214),
public/index.html (246), public/mchd.html (204), public/app.css, public/login.html,
а также test/mchd.test.js и test/zipFixture.js.

Ищи: длинные функции, дублирование между index.html и mchd.html (страницы писались похоже),
разбор ZIP и XML своими руками — там легко ошибиться: границы буфера, zip-bomb,
вредоносные имена файлов внутри архива, переполнение, некорректный deflate, вложенные архивы.
Проверь parseCertificate на устойчивость к кривым сертификатам.
Проверь режим BEHIND_GATEWAY: что именно он отключает и не открывает ли лишнего;
что будет, если модуль запустят с BEHIND_GATEWAY=true, но открытым наружу портом.

Особое внимание на чтение ZIP по смещениям из НЕДОВЕРЕННОГО файла: может ли
специально собранный .zip уронить процесс, съесть всю память или прочитать не своё.
Помни ограничение проекта: из МЧД сохраняются только ФИО, реестровый номер и срок — намеренно.`,
  },
  {
    key: 'smdr',
    label: 'журнал звонков',
    prompt: `Разбери SMDR: web-node/server.js (197), web-node/db.js (20), web-node/views/*.ejs,
web-node/public/style.css, web-node/public/select.js, Collector.Py (~250).

В web-node ищи: SQL-ИНЪЕКЦИИ (запросы собираются конкатенацией строк — проверь каждую),
отсутствие авторизации (модуль слушает 127.0.0.1 — достаточно ли этого и так ли это на самом деле),
неограниченные выборки (SELECT без LIMIT на вечно растущей таблице calls),
N+1, агрегацию в JS вместо SQL, XSS в EJS (<%= против <%-), обработку дат из запроса.

В Collector.Py ищи: обработку обрывов TCP, кодировки, утечку ресурсов, что происходит
при неверном формате строки SMDR, разбор длительности, поведение при переполнении диска.
Логин и пароль АТС внутри скрипта — осознанное решение владельца, НЕ дефект;
но если пароль попадает в лог — это дефект, отметь.`,
  },
  {
    key: 'messenger',
    label: 'Искра: только безопасность и наши правки',
    prompt: `MESSENGER/ — вендоренная копия чужого репозитория, её сливают с upstream.
СТРУКТУРНЫЙ РЕФАКТОРИНГ ТАМ ПРЕДЛАГАТЬ НЕЛЬЗЯ — это создаст вечный конфликт слияния.

Смотри только на две вещи:
  1. ДЕФЕКТЫ БЕЗОПАСНОСТИ, которые видны в MESSENGER/server.js (2318) и public/index.html (1744):
     проверка JWT, права администратора, SQL, XSS, загрузка файлов, раздача обновлений клиента
     (MESSENGER/updates/), хранение паролей. Это внутренняя сеть, но учётки настоящие.
  2. НАШИ СОБСТВЕННЫЕ ПРАВКИ — их можно найти по git log/git blame:
     presenceSnapshot() и /api/admin/presence в server.js, разметка и CSS списка
     пользователей в public/index.html (.user-row, .dept-block, перетаскивание отделов,
     группировка по отделам). Здесь ищи обычные дефекты: гонки, утечки обработчиков,
     неверную сортировку, доступность с клавиатуры, поведение при пустом списке.

Каждую находку помечай в title префиксом «[наше]» или «[upstream]».`,
  },
  {
    key: 'cross',
    label: 'сквозные проблемы',
    prompt: `Смотри НА ВЕСЬ проект сразу и ищи то, что видно только поверх файлов.
Проверяй утверждения командами (grep/find/wc), не полагайся на впечатление.

1. ДУБЛИРОВАНИЕ между компонентами: одинаковый разбор .env? одинаковые хелперы дат
   и экранирования в четырёх фронтендах? одинаковые палитры CSS? одинаковые кастомные
   выпадающие списки (их точно несколько)? одинаковая проверка сессии?
   Найди всё и честно оцени, что реально стоит выносить, а что дублировать ДЕШЕВЛЕ:
   компоненты — отдельные процессы с отдельными package.json, общей библиотеки нет,
   и заводить её ради трёх функций может быть хуже дублирования.

2. МЁРТВЫЙ КОД: экспорты, которые никто не импортирует; функции, которые никто не зовёт;
   CSS-классы, которых нет в разметке; зависимости в package.json, которые не используются
   (проверь КАЖДУЮ по имени через grep); файлы-сироты; закомментированные куски.

3. ХАРДКОД: порты, пути, доменные имена, адреса почты, магические числа —
   что должно быть настройкой, а что законно захардкожено.

4. КОНСИСТЕНТНОСТЬ: одни и те же понятия названы по-разному? разные стили обработки
   ошибок в соседних файлах? разные соглашения об именах? разный формат ответов API?

5. ДОКУМЕНТАЦИЯ И ВЫКЛАДКА: README.md в корне и в каждом компоненте, .env.example —
   соответствуют ли они коду? Есть ли переменные в коде, которых нет в .env.example,
   и наоборот? Правильны ли инструкции по запуску?`,
  },
  {
    key: 'perf',
    label: 'производительность и схема данных',
    prompt: `Смотри на весь проект с точки зрения производительности, объёмов и схемы данных.

1. СХЕМА: helpdesk-backend/db/schema.sql, CREATE TABLE в CERTVIEWER/server.js,
   SMDR/web-node/db.js. Есть ли индексы под ФАКТИЧЕСКИЕ запросы (найди запросы grep-ом
   и сопоставь с индексами)? Есть ли лишние индексы? Правильные типы и ограничения?
   Внешние ключи и ON DELETE? Включён ли foreign_keys вообще (в SQLite он по умолчанию ВЫКЛЮЧЕН)?

2. ЗАПРОСЫ: найди N+1 (запрос в цикле), выборки без LIMIT, агрегацию в JS там, где
   должен агрегировать SQL, повторную подготовку одного и того же prepared statement
   в горячем пути, COUNT(*) на каждой отрисовке.

3. СИНХРОННОСТЬ: node:sqlite синхронный, better-sqlite3 тоже. Где длинные синхронные
   операции блокируют цикл событий на пути запроса? fs.*Sync в обработчиках?
   Синхронный bcrypt? Разбор большого ZIP в обработчике?

4. РОСТ: calls в SMDR растёт вечно; notification_events и notification_deliveries тоже;
   uploads/ тоже. Есть ли план ротации/очистки? Что сломается на 100 тыс. записей?
   Посчитай примерный объём за год при реальных масштабах.

5. ФРОНТЕНД: опрос по таймеру — сколько запросов в минуту с одного рабочего места,
   что тянет каждый, можно ли дешевле. Размер отдаваемых страниц, кэширование статики.`,
  },
  {
    key: 'tests',
    label: 'качество существующих тестов',
    prompt: `Разбери НЕ код, а ТЕСТЫ: helpdesk-backend/test/*.test.js (56 тестов),
helpdesk-backend/test/helpers/*.js, CERTVIEWER/test/mchd.test.js (23), CERTVIEWER/test/zipFixture.js.

Ищи ЛОЖНУЮ УВЕРЕННОСТЬ — тесты, которые зелены, но ничего не доказывают:
  - тесты, которые проверяют то, что не может сломаться (геттеры, константы);
  - тесты без assert на главном свойстве (проверяют, что не упало, а не что сделано верно);
  - моки, которые повторяют реализацию, а не проверяют её;
  - общее состояние между тестами (одна база на все, порядок выполнения важен);
  - тесты, которые пройдут даже если сломать код (проверь мысленно: закомментируй строку
    в коде — упадёт ли тест? Для самых важных проверок ПРОВЕРЬ ЭТО РЕАЛЬНО:
    внеси правку, запусти npm test, верни правку обратно);
  - висящие ресурсы (сервер/таймер/база не закрыты — тест проходит, но процесс живёт);
  - зависимость от текущей даты и часового пояса (это самая частая причина «в пятницу упало»);
  - недетерминированность, гонки, таймауты.

Отдельно перечисли, ЧТО СЕЙЧАС НЕ ПОКРЫТО и является критическим путём.
Категория для таких находок — test-gap.

ВАЖНО: если вносишь правку в код проекта для проверки — ОБЯЗАТЕЛЬНО верни файл
в исходное состояние (git checkout -- <файл>) и убедись, что npm test снова зелёный.`,
  },
]

// Карта и поиск идут одновременно: направлениям поиска карта не нужна,
// а ждать её — это впустую потраченное время на самой длинной стадии.
const mapWork = (async () => {
  const parts = await parallel(MAP_READERS.map((r) => () => agent(
    `${CONTEXT}\n\nТЫ ЧИТАЕШЬ УЧАСТОК: ${r.key}\n\n${r.prompt}\n\n` +
    `Читай реальные файлы целиком, не по именам. Верни плотный русский текст, 500-800 слов, без воды.`,
    { label: `карта:${r.key}`, phase: 'Карта' }
  )))
  const good = parts.filter(Boolean)
  if (!good.length) return ''
  return agent(
    `${CONTEXT}\n\nТри агента прочитали разные части репозитория. Сведи их наблюдения в ОДНУ карту архитектуры.\n` +
    `Где они противоречат друг другу — открой файл и проверь сам, кто прав.\n\n` +
    `=== ЯДРО ===\n${good[0] || ''}\n\n=== СЕРВИСЫ ===\n${good[1] || ''}\n\n=== МОДУЛИ И ФРОНТ ===\n${good[2] || ''}\n\n` +
    `Верни на русском:\n` +
    `1. Как компоненты связаны: кто кого зовёт, где границы процессов, где проходят доверительные границы.\n` +
    `2. Слои внутри helpdesk-backend — соблюдаются ли, где протекают, насколько это мешает.\n` +
    `3. Где живёт состояние: базы, файлы, таблица settings как скрытая шина.\n` +
    `4. Что сделано ХОРОШО и почему это стоит сохранить (будь конкретен, это не вежливость).\n` +
    `5. Что в архитектуре вызывает вопросы и чем это обернётся при росте.\n` +
    `900-1300 слов, плотно.`,
    { label: 'карта:сведение', phase: 'Карта' }
  )
})()

const searchWork = pipeline(
  AREAS,
  (area) => agent(
    `${CONTEXT}\n\nТЫ РАЗБИРАЕШЬ УЧАСТОК: ${area.label}\n\n${area.prompt}\n\n` +
    `ПРАВИЛА ОТВЕТА:\n` +
    `- Читай реальный код целиком, не догадывайся. Проверяй утверждения grep-ом и запуском.\n` +
    `- Каждая находка КОНКРЕТНА: файл, строка, что не так, чем обернётся.\n` +
    `- Поле fix — ГОТОВЫЙ НОВЫЙ КОД, который можно вставить, с русскими комментариями про ПОЧЕМУ.\n` +
    `- Не выдумывай проблемы ради количества. Пять настоящих лучше двадцати придуманных.\n` +
    `- Прежде чем объявить дефектом — прочитай комментарии вокруг: в этом проекте много\n` +
    `  осознанных решений, и они объяснены прямо в коде.\n` +
    `- severity=critical только для того, что уже сейчас ломается или пробивает защиту.\n` +
    `- Ничего не меняй в файлах проекта (кроме случая, описанного в задании про тесты).`,
    { label: `поиск:${area.key}`, phase: 'Поиск', schema: FINDING_SCHEMA }
  ),
  (found, area) => {
    if (!found || !found.findings || !found.findings.length) {
      return { area: area.key, label: area.label, found, verdicts: { verdicts: [] } }
    }
    return agent(
      `${CONTEXT}\n\nТЫ — СКЕПТИК. Другой агент разобрал участок «${area.label}» и выдал находки.\n` +
      `Твоя задача — ОПРОВЕРГНУТЬ каждую. По умолчанию находка ложная, пока ты не убедился в обратном,\n` +
      `открыв файл и прочитав код вокруг указанной строки.\n\n` +
      `Проверяй по пунктам:\n` +
      `1. Существует ли описанное в коде вообще? Может, агент выдумал строку или перепутал файл.\n` +
      `2. Правда ли это приводит к описанному следствию? Может, дальше по коду есть проверка,\n` +
      `   которую он не заметил, или вызывающий уже всё делает.\n` +
      `3. Не является ли это ОСОЗНАННЫМ решением проекта? Прочитай комментарии рядом.\n` +
      `4. Корректен ли предложенный код? Выполнится ли, не сломает ли соседнее, не нарушает ли\n` +
      `   ограничения проекта (нет сборки, нет новых зависимостей, Chromium 109, синхронный node:sqlite).\n` +
      `5. Не завышена ли severity? critical — только то, что ломается прямо сейчас или пробивает защиту.\n\n` +
      `НАХОДКИ:\n${JSON.stringify(found.findings, null, 1)}\n\n` +
      `Верни вердикт по КАЖДОМУ id, ничего не пропуская.`,
      { label: `проверка:${area.key}`, phase: 'Проверка', schema: VERDICT_SCHEMA }
    ).then((v) => ({ area: area.key, label: area.label, found, verdicts: v }))
  }
)

const [map, results] = await Promise.all([mapWork, searchWork])

const clean = results.filter(Boolean)
const survived = []
const rejected = []
for (const r of clean) {
  const byId = new Map((r.verdicts && r.verdicts.verdicts ? r.verdicts.verdicts : []).map((v) => [v.id, v]))
  for (const f of (r.found && r.found.findings) || []) {
    const v = byId.get(f.id) || null
    const sev = (v && v.severity_corrected) || f.severity
    const entry = { ...f, severity: sev, area: r.area, area_label: r.label, skeptic: v }
    if (!v || v.real) survived.push(entry)
    else rejected.push(entry)
  }
}
log(`после скептика: подтверждено ${survived.length}, опровергнуто ${rejected.length}`)

// Серьёзные находки проходят ещё три независимых взгляда. Дешёвые (medium/low)
// туда не идут: цена ошибки в них мала, а агентов они съедят столько же.
const heavy = survived.filter((f) => f.severity === 'critical' || f.severity === 'high')
const light = survived.filter((f) => f.severity !== 'critical' && f.severity !== 'high')
log(`на тройную экспертизу идут ${heavy.length} находок (critical/high); ${light.length} остаются с одним вердиктом`)

const LENSES = [
  {
    key: 'воспроизведение',
    ask: `Ты проверяешь ВОСПРОИЗВОДИМОСТЬ. Составь конкретный сценарий: какие входные данные,\n` +
         `какая последовательность действий, какой неверный результат. Пройди по коду руками, строка за строкой.\n` +
         `Если у тебя не выходит связный сценарий — значит находка не подтверждена, refuted=true.`,
  },
  {
    key: 'контекст',
    ask: `Ты проверяешь ОСОЗНАННОСТЬ. Прочитай комментарии вокруг этого места, README компонента,\n` +
         `историю (git log -p по файлу). Возможно, так сделано намеренно и причина записана.\n` +
         `Если это осознанное решение проекта, а не дефект — refuted=true.`,
  },
  {
    key: 'правка',
    ask: `Ты проверяешь ПРЕДЛОЖЕННЫЙ КОД. Мысленно примени его к файлу: выполнится ли, не сломает ли\n` +
         `соседнее, не разойдётся ли с вызывающими, не нарушает ли ограничений проекта,\n` +
         `не сломает ли существующие 79 тестов. Заполни fix_ok и объясни.\n` +
         `refuted=true ставь только если, применив правку, ты понял, что исходной проблемы не было.`,
  },
]

const judged = await parallel(heavy.map((f) => () =>
  parallel(LENSES.map((lens) => () => agent(
    `${CONTEXT}\n\nТЫ — НЕЗАВИСИМЫЙ ЭКСПЕРТ. Проверяешь ОДНУ находку из аудита.\n` +
    `${lens.ask}\n\n` +
    `НАХОДКА:\n${JSON.stringify({
      title: f.title, severity: f.severity, category: f.category,
      file: f.file, line: f.line, now: f.now, why_bad: f.why_bad,
      failure_scenario: f.failure_scenario, fix: f.fix,
    }, null, 1)}\n\n` +
    `Открой файл ${f.file} и прочитай код вокруг строки ${f.line}. Не верь описанию на слово.`,
    { label: `экспертиза:${f.id}:${lens.key}`, phase: 'Экспертиза', schema: LENS_SCHEMA }
  ))).then((votes) => {
    const good = votes.filter(Boolean)
    const against = good.filter((v) => v.refuted).length
    const corrected = good.map((v) => v.severity_corrected).filter(Boolean)
    return {
      ...f,
      lenses: good,
      // Опровергнута, если против высказалось большинство. Один несогласный
      // из трёх — это нормальный разброс, а не приговор находке.
      confirmed: against < Math.ceil(good.length / 2),
      severity: corrected.length ? corrected.sort((a, b) =>
        ['critical', 'high', 'medium', 'low'].indexOf(a) - ['critical', 'high', 'medium', 'low'].indexOf(b)
      )[Math.floor(corrected.length / 2)] : f.severity,
      fix_ok: good.every((v) => v.fix_ok !== false),
    }
  })
))

const heavyOk = judged.filter(Boolean).filter((f) => f.confirmed)
const heavyOut = judged.filter(Boolean).filter((f) => !f.confirmed)
log(`после тройной экспертизы: осталось ${heavyOk.length} серьёзных, отброшено ${heavyOut.length}`)

const confirmed = [...heavyOk, ...light]

phase('Полнота')
const gaps = await agent(
  `${CONTEXT}\n\nТы — КРИТИК ПОЛНОТЫ аудита. Ниже — что аудит посмотрел и что нашёл.\n` +
  `Твоя задача: найти, ЧТО ОСТАЛОСЬ НЕПОСМОТРЕННЫМ.\n\n` +
  `Направления, по которым шёл поиск: ${AREAS.map((a) => a.label).join(', ')}.\n\n` +
  `Краткие оценки участков:\n${clean.map((r) => `- ${r.label}: ${(r.found && r.found.summary) || '—'}`).join('\n')}\n\n` +
  `Подтверждённые находки (только заголовки):\n${confirmed.map((f) => `- [${f.severity}] ${f.file}: ${f.title}`).join('\n')}\n\n` +
  `Пройди по репозиторию сам (find/grep) и ответь:\n` +
  `1. Какие ФАЙЛЫ не попали ни в одно направление? Перечисли и скажи, стоит ли их смотреть.\n` +
  `2. Какие КЛАССЫ проблем не искали вовсе (например: доступность, локализация, логирование,\n` +
  `   восстановление после сбоя, миграции схемы, поведение при обновлении на работающей системе,\n` +
  `   резервное копирование, поведение на Windows против Linux — пути, разделители, кодировки)?\n` +
  `3. Какие утверждения аудита выглядят непроверенными и стоит перепроверить?\n` +
  `Отвечай конкретно, со ссылками на файлы. 400-700 слов.`,
  { label: 'полнота', phase: 'Полнота' }
)

const order = { critical: 0, high: 1, medium: 2, low: 3 }
confirmed.sort((a, b) => (order[a.severity] ?? 9) - (order[b.severity] ?? 9))

return {
  map,
  summaries: clean.map((r) => ({ area: r.area, label: r.label, summary: (r.found && r.found.summary) || '' })),
  confirmed,
  rejected: [
    ...rejected.map((r) => ({ id: r.id, file: r.file, title: r.title, why: (r.skeptic && r.skeptic.reason) || '' })),
    ...heavyOut.map((r) => ({ id: r.id, file: r.file, title: r.title, why: r.lenses.filter((l) => l.refuted).map((l) => l.note).join(' | ') })),
  ],
  gaps,
  counts: {
    всего_найдено: survived.length + rejected.length,
    подтверждено: confirmed.length,
    опровергнуто: rejected.length + heavyOut.length,
  },
}
