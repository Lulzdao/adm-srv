export const meta = {
  name: 'adm-srv-audit-full',
  description: 'Добор аудита adm-srv: модули, сквозные проблемы, производительность, TLS, качество тестов',
  phases: [
    { title: 'Поиск', detail: 'восемь направлений, не покрытых первым заходом' },
    { title: 'Проверка', detail: 'скептик опровергает находки своего участка' },
  ],
}

const CONTEXT = `
ПРОЕКТ: /home/user/adm-srv — внутренняя ИТ-платформа Липецкстата (территориальный
орган Росстата). Изолированная сеть без интернета, Windows Server 2016, Node.js 22.
Организация ~100-200 сотрудников, несколько заявок в день, тысячи звонков в месяц.

Состав:
  helpdesk-backend/  — платформа: заявки, вход через LDAP, прокси модулей, оповещения.
                       server.js 85, routes/tickets.js 579, routes/notifications.js 393,
                       routes/certificates.js 248, routes/auth.js 169, routes/modules.js 110,
                       routes/admin.js 82, services/tls.js 366, services/notifications.js 256,
                       services/mailer.js 214, services/ldapAuth.js 161, services/scheduler.js 148,
                       services/sources/certs.js 187, services/sources/smdr.js 66,
                       services/moduleClient.js 57, config/notifications.js 176,
                       db/schema.sql 161, db/init.js 135, public/app.js 2034, public/styles.css 473
  CERTVIEWER/        — модуль: реестр сертификатов и МЧД (server.js 466, mchd.js 214)
  SMDR/              — модуль: журнал звонков (web-node/server.js 197, db.js 20, Collector.Py ~250)
  MESSENGER/         — мессенджер «Искра»: server.js 2318, public/index.html 1744, Electron-клиент

ЖЁСТКИЕ ОГРАНИЧЕНИЯ — предложения, которые их нарушают, бесполезны:
  1. НЕТ шага сборки. Фронтенд — чистые HTML/CSS/JS. React рассматривали и отвергли.
  2. НЕТ новых npm-зависимостей без крайней нужды: сеть изолирована, node-gyp не соберётся.
     Поэтому node:sqlite (DatabaseSync — СИНХРОННЫЙ), node:test, встроенный zlib, разбор ZIP руками.
  3. Браузеры — Chromium 109. Никакого oklch, :has(), веб-шрифтов.
  4. MESSENGER/ — ВЕНДОРЕННАЯ КОПИЯ Lulzdao/Iskra-messenger, её сливают с upstream.
     Структурный рефакторинг там НЕ предлагать. Только безопасность и наши собственные правки.
  5. Комментарии на русском объясняют ПОЧЕМУ. Сохраняй этот стиль в предлагаемом коде.
  6. Осознанные решения, НЕ являющиеся недоделками:
     - из архива МЧД хранятся ТОЛЬКО ФИО, реестровый номер и срок (паспорт/СНИЛС/ИНН — намеренно нет);
     - логин и пароль АТС внутри SMDR/Collector.Py, а не в .env — решение владельца;
     - модули слушают 127.0.0.1 и доверяют запросам за платформой (CERTVIEWER при BEHIND_GATEWAY=true).

ТЕСТЫ ЕСТЬ — 79 штук на встроенном node:test, все зелёные:
  helpdesk-backend/test/{notifications 16, expiry 16, scheduler 14, localAccounts 7}.test.js
  helpdesk-backend/test/helpers/{tempDb,fakeSmtp,fakeModules}.js — оснастка
  CERTVIEWER/test/mchd.test.js 23, CERTVIEWER/test/zipFixture.js
Запуск: npm test (это "node --test" без аргумента).
ИЗМЕРЕНО: тесты загружают лишь 1449 из 3755 строк серверного кода платформы (39%).
Совсем не загружаются: все routes/*, middleware/auth.js, services/tls.js,
services/ldapAuth.js, services/userStore.js, services/network.js, server.js, public/app.js,
CERTVIEWER/server.js. Внутри загруженного покрытие строк 96%.

ПЕРВЫЙ ЗАХОД АУДИТА УЖЕ ПРОШЁЛ по участкам: заявки, фронтенд платформы, сервисы,
маршруты и вход. Их находки собраны, ПОВТОРЯТЬ ИХ НЕ НУЖНО. В частности, уже
найдено и подтверждено чтением кода — не сообщай об этом снова:
  - getTicketDetail отдаёт внутренние заметки (is_internal) заявителю;
  - esc() в public/app.js не экранирует кавычки, а стоит внутри атрибутов —
    хранимая XSS через имя вложения (в БД пишется несанированный originalname);
  - ограничение попыток входа обходится параллельными запросами;
  - nextDisplayId через COUNT(*) при UNIQUE display_id;
  - tick() планировщика не защищён от наложения на себя;
  - deliverPending не сериализован.

ЗАПРЕТ НА ДАННЫЕ: в проекте есть архивы с персональными данными. НИКОГДА не приводи
настоящие ФИО, СНИЛС, ИНН, паспорта, реестровые номера, адреса почты сотрудников.
Все примеры — выдуманные. Файлы helpdesk-backend/data/*.db и SMDR/*.db не читай.
`

const FINDING_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['area', 'summary', 'findings'],
  properties: {
    area: { type: 'string' },
    summary: { type: 'string', description: 'Честная оценка участка, 3-5 предложений: что хорошо, что болит' },
    findings: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['id', 'title', 'severity', 'category', 'file', 'line', 'now', 'why_bad', 'fix', 'failure_scenario', 'effort'],
        properties: {
          id: { type: 'string' },
          title: { type: 'string' },
          severity: { type: 'string', enum: ['critical', 'high', 'medium', 'low'] },
          category: {
            type: 'string',
            enum: ['bug', 'security', 'architecture', 'duplication', 'naming', 'dead-code',
                   'error-handling', 'performance', 'hardcode', 'complexity', 'leaky-abstraction', 'test-gap'],
          },
          file: { type: 'string' },
          line: { type: 'integer' },
          now: { type: 'string', description: 'Что сейчас — кратко, с точным фрагментом кода' },
          why_bad: { type: 'string', description: 'Конкретное следствие, не «нарушает принципы»' },
          fix: { type: 'string', description: 'ГОТОВЫЙ НОВЫЙ КОД, с русскими комментариями про ПОЧЕМУ' },
          failure_scenario: { type: 'string', description: 'Для bug/security/performance: входные данные -> неверный результат. Иначе пустая строка' },
          effort: { type: 'string', enum: ['минуты', 'часы', 'день+'] },
        },
      },
    },
  },
}

const VERDICT_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['verdicts'],
  properties: {
    verdicts: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['id', 'real', 'reason', 'fix_ok'],
        properties: {
          id: { type: 'string' },
          real: { type: 'boolean' },
          severity_corrected: { type: 'string', enum: ['critical', 'high', 'medium', 'low', ''] },
          reason: { type: 'string', description: 'Что именно проверил и что увидел в коде' },
          fix_ok: { type: 'boolean' },
          fix_problem: { type: 'string' },
        },
      },
    },
  },
}

const AREAS = [
  {
    key: 'certviewer',
    label: 'Сертвивер',
    prompt: `Разбери модуль CERTVIEWER целиком: server.js (466), mchd.js (214),
public/index.html (246), public/mchd.html (204), public/app.css, public/login.html.

Ищи: длинные функции, дублирование между index.html и mchd.html, разбор ZIP и XML
своими руками — границы буфера, zip-bomb, вредоносные имена файлов внутри архива,
переполнение, некорректный deflate, вложенные архивы. Устойчивость parseCertificate
к кривым сертификатам. Режим BEHIND_GATEWAY: что именно отключает, не открывает ли лишнего,
что будет при BEHIND_GATEWAY=true и открытом наружу порте.
Отдельно: SQL-запросы, отдача файлов, лимиты загрузки, XSS в трёх страницах.

Особое внимание на чтение ZIP по смещениям из НЕДОВЕРЕННОГО файла: может ли
специально собранный .zip уронить процесс, съесть память или прочитать не своё.
Помни: из МЧД сохраняются только ФИО, реестровый номер и срок — это намеренно.`,
  },
  {
    key: 'smdr',
    label: 'журнал звонков',
    prompt: `Разбери SMDR: web-node/server.js (197), web-node/db.js (20), web-node/views/*.ejs,
web-node/public/style.css, web-node/public/select.js, Collector.Py (~250).

В web-node ищи: SQL-ИНЪЕКЦИИ (запросы собираются конкатенацией — проверь КАЖДУЮ),
отсутствие авторизации (слушает 127.0.0.1 — проверь, действительно ли, и достаточно ли),
неограниченные выборки на вечно растущей таблице calls, N+1, агрегацию в JS вместо SQL,
XSS в EJS (<%= против <%-), обработку дат из запроса, POST-маршруты без всякой проверки.

В Collector.Py: обработку обрывов TCP, кодировки, утечку ресурсов, неверный формат
строки SMDR, разбор длительности, поведение при переполнении диска, попадание пароля в лог.
Логин и пароль АТС внутри скрипта — осознанное решение владельца, НЕ дефект сам по себе.`,
  },
  {
    key: 'tls',
    label: 'сертификаты сервера и TLS',
    prompt: `Разбери helpdesk-backend/services/tls.js (366 — самый большой сервис, не покрыт тестами)
и helpdesk-backend/routes/certificates.js (248).

Это код, управляющий сертификатами и закрытыми ключами. Ищи:
  - работу с путями из пользовательского ввода (обход каталога при загрузке/удалении/чтении);
  - попадание закрытых ключей и паролей в логи и в ответы API;
  - разбор PFX/PEM: битый файл, неверный пароль, огромный файл, файл-бомба;
  - поведение при отсутствии файла, недоступном хранилище, гонке (перезапись во время чтения);
  - fs.watch и setSecureContext: что при удалении файла, при частых изменениях, при битом новом файле;
  - обработку ошибок: что увидит пользователь, что попадёт в консоль;
  - смешение обязанностей и как их разделить;
  - валидацию загрузки: размер, тип, имя.
Для каждой находки security обязателен failure_scenario.`,
  },
  {
    key: 'messenger',
    label: 'Искра: безопасность и наши правки',
    prompt: `MESSENGER/ — вендоренная копия чужого репозитория. СТРУКТУРНЫЙ РЕФАКТОРИНГ НЕ ПРЕДЛАГАТЬ.

Смотри только на две вещи:
  1. ДЕФЕКТЫ БЕЗОПАСНОСТИ в MESSENGER/server.js (2318) и public/index.html (1744):
     проверка JWT (алгоритм, срок, секрет), права администратора, SQL, XSS,
     загрузка файлов, раздача обновлений клиента (MESSENGER/updates/), хранение паролей,
     WebSocket-аутентификация. Сеть внутренняя, но учётки настоящие.
  2. НАШИ СОБСТВЕННЫЕ ПРАВКИ (найди их через git log/git blame по файлу):
     presenceSnapshot() и /api/admin/presence в server.js; разметка и CSS списка
     пользователей в public/index.html (.user-row, .dept-block, перетаскивание отделов,
     группировка по отделам, applyDeptOrder). Здесь ищи гонки, утечки обработчиков,
     неверную сортировку, доступность с клавиатуры, поведение при пустом списке,
     откат при отказе сервера.

Каждую находку помечай в title префиксом «[наше]» или «[upstream]».`,
  },
  {
    key: 'cross',
    label: 'сквозные проблемы',
    prompt: `Смотри НА ВЕСЬ проект сразу. Проверяй утверждения командами (grep/find/wc), не на глаз.

1. ДУБЛИРОВАНИЕ между компонентами: разбор .env? хелперы дат и экранирования в четырёх
   фронтендах? палитры CSS? кастомные выпадающие списки (их точно несколько)? проверка сессии?
   Найди всё и честно оцени, что стоит выносить, а что дублировать ДЕШЕВЛЕ: компоненты —
   отдельные процессы с отдельными package.json, общей библиотеки нет, и заводить её ради
   трёх функций может быть хуже дублирования.
2. МЁРТВЫЙ КОД: экспорты, которые никто не импортирует; функции, которые никто не зовёт;
   CSS-классы, которых нет в разметке; зависимости в package.json, не используемые в коде
   (проверь КАЖДУЮ через grep); файлы-сироты; закомментированные куски.
3. ХАРДКОД: порты, пути, доменные имена, адреса почты, магические числа — что должно быть
   настройкой, а что законно захардкожено.
4. КОНСИСТЕНТНОСТЬ: одни понятия названы по-разному? разные стили обработки ошибок?
   разный формат ответов API? разные соглашения об именах?
5. ДОКУМЕНТАЦИЯ И ВЫКЛАДКА: README.md в корне и в компонентах, .env.example — соответствуют ли
   коду? Есть ли переменные в коде, которых нет в .env.example, и наоборот? Верны ли инструкции запуска?
6. WINDOWS: проект живёт на Windows Server 2016. Есть ли места, где путь собирается через "/",
   где важен регистр имён файлов, где кодировка консоли ломает вывод, где fs.watch ведёт себя иначе?`,
  },
  {
    key: 'perf',
    label: 'производительность и схема данных',
    prompt: `Смотри на весь проект с точки зрения производительности, объёмов и схемы данных.

1. СХЕМА: helpdesk-backend/db/schema.sql, CREATE TABLE в CERTVIEWER/server.js, SMDR/web-node/db.js.
   Есть ли индексы под ФАКТИЧЕСКИЕ запросы (найди запросы grep-ом и сопоставь)? Лишние индексы?
   Верные типы и ограничения? Внешние ключи и ON DELETE? Включён ли PRAGMA foreign_keys
   (в SQLite он по умолчанию ВЫКЛЮЧЕН — проверь каждую базу отдельно)? WAL?
2. ЗАПРОСЫ: N+1 (запрос в цикле), выборки без LIMIT, агрегация в JS вместо SQL,
   повторная подготовка одного prepared statement в горячем пути, COUNT(*) на каждой отрисовке.
3. СИНХРОННОСТЬ: node:sqlite и better-sqlite3 синхронны. Где длинные синхронные операции
   блокируют цикл событий на пути запроса? fs.*Sync в обработчиках? синхронный bcrypt?
   разбор большого ZIP в обработчике? отправка почты внутри обработки запроса?
4. РОСТ: calls в SMDR растёт вечно; notification_events и notification_deliveries тоже;
   uploads/ тоже. Есть ли ротация? Что сломается на 100 тыс. записей? Посчитай объём за год
   при реальных масштабах (несколько заявок в день, тысячи звонков в месяц).
5. ФРОНТЕНД: опрос по таймеру — сколько запросов в минуту с рабочего места, что тянет каждый,
   можно ли дешевле. Размер отдаваемых страниц, кэширование статики, отсутствие ETag.`,
  },
  {
    key: 'tests',
    label: 'качество существующих тестов',
    prompt: `Разбери НЕ код, а ТЕСТЫ: helpdesk-backend/test/*.test.js (56), test/helpers/*.js,
CERTVIEWER/test/mchd.test.js (23), CERTVIEWER/test/zipFixture.js.

Ищи ЛОЖНУЮ УВЕРЕННОСТЬ — тесты, которые зелены, но ничего не доказывают:
  - проверяют то, что не может сломаться (константы, геттеры);
  - без assert на главном свойстве (проверяют, что не упало, а не что сделано верно);
  - моки, повторяющие реализацию вместо её проверки;
  - общее состояние между тестами, зависимость от порядка;
  - тесты, которые пройдут, даже если сломать код. ДЛЯ 5-7 САМЫХ ВАЖНЫХ ПРОВЕРЬ ЭТО РЕАЛЬНО:
    внеси ломающую правку в код, запусти npm test, убедись, упал ли тест, и ОБЯЗАТЕЛЬНО
    верни файл через git checkout -- <файл>, затем убедись, что npm test снова зелёный.
    В поле now пиши результат этого опыта: «сломал X — тест не заметил» или «сломал X — упало 3 теста».
  - висящие ресурсы (сервер/таймер/база не закрыты);
  - зависимость от текущей даты и часового пояса — самая частая причина «в пятницу упало».
    Проверь запуском: TZ=Pacific/Kiritimati npm test и TZ=Pacific/Niue npm test (UTC+14 и UTC-11).
  - недетерминированность и гонки.

Отдельно перечисли, ЧТО НЕ ПОКРЫТО и является критическим путём (категория test-gap).
ОБЯЗАТЕЛЬНО: в конце убедись, что рабочее дерево чистое (git status) и npm test зелёный в обоих каталогах.`,
  },
  {
    key: 'ops',
    label: 'эксплуатация и восстановление',
    prompt: `Смотри на проект глазами того, кто его эксплуатирует на одном Windows-сервере без ИТ-отдела.

1. ЗАПУСК И ПАДЕНИЕ: что происходит при старте, если БД повреждена, каталог uploads недоступен,
   порт занят, .env отсутствует, домен недоступен? Процесс падает с понятным сообщением
   или сыпет стеком? Есть ли необработанные исключения и unhandledRejection?
2. МИГРАЦИИ СХЕМЫ: как накатывается изменение schema.sql на работающую базу? Что произойдёт
   при обновлении версии на боевом сервере — данные переживут? Есть ли версия схемы?
   Посмотри db/init.js и migrateNotifications: идемпотентны ли, что при частичном применении?
3. РЕЗЕРВНОЕ КОПИРОВАНИЕ: можно ли скопировать .db на живой системе (WAL!), есть ли инструкция?
4. ЛОГИ: куда пишутся, ротируются ли, попадают ли туда пароли и персональные данные,
   можно ли по логу понять, что случилось у пользователя?
5. ДИАГНОСТИКА: если пользователь говорит «письмо не пришло» — есть ли чем это посмотреть?
   Если «модуль не открывается» — видно ли, почему?
6. ОБНОВЛЕНИЕ: описан ли порядок выкладки, останавливаются ли службы, что с открытыми сессиями?

Читай README.md в корне и в каждом компоненте, .env.example, db/init.js, server.js каждого
компонента, обработчики ошибок. Находки — конкретные, с файлом и строкой.`,
  },
]

phase('Поиск')
const results = await pipeline(
  AREAS,
  (area) => agent(
    `${CONTEXT}\n\nТЫ РАЗБИРАЕШЬ УЧАСТОК: ${area.label}\n\n${area.prompt}\n\n` +
    `ПРАВИЛА ОТВЕТА:\n` +
    `- Читай реальный код целиком, не догадывайся. Проверяй утверждения grep-ом и запуском.\n` +
    `- Каждая находка КОНКРЕТНА: файл, строка, что не так, чем обернётся.\n` +
    `- Поле fix — ГОТОВЫЙ НОВЫЙ КОД, который можно вставить, с русскими комментариями про ПОЧЕМУ.\n` +
    `- Не выдумывай проблемы ради количества. Пять настоящих лучше двадцати придуманных.\n` +
    `- Прежде чем объявить дефектом — прочитай комментарии вокруг: в этом проекте много\n` +
    `  осознанных решений, и они объяснены прямо в коде.\n` +
    `- severity=critical только для того, что уже сейчас ломается или пробивает защиту.\n` +
    `- Не меняй файлы проекта (кроме опытов, описанных в задании про тесты, — там возвращай всё назад).`,
    { label: `поиск:${area.key}`, phase: 'Поиск', schema: FINDING_SCHEMA }
  ),
  (found, area) => {
    if (!found || !found.findings || !found.findings.length) {
      return { area: area.key, label: area.label, found, verdicts: { verdicts: [] } }
    }
    return agent(
      `${CONTEXT}\n\nТЫ — СКЕПТИК. Другой агент разобрал участок «${area.label}» и выдал находки.\n` +
      `Твоя задача — ОПРОВЕРГНУТЬ каждую. По умолчанию находка ложная, пока ты не убедился в обратном,\n` +
      `открыв файл и прочитав код вокруг указанной строки.\n\n` +
      `1. Существует ли описанное в коде вообще? Может, агент выдумал строку или перепутал файл.\n` +
      `2. Правда ли это приводит к описанному следствию? Может, дальше есть проверка, которую он не заметил.\n` +
      `3. Не осознанное ли это решение проекта? Прочитай комментарии рядом.\n` +
      `4. Корректен ли предложенный код? Выполнится ли, не сломает ли соседнее, не нарушает ли\n` +
      `   ограничений проекта, не уронит ли существующие 79 тестов.\n` +
      `5. Не завышена ли severity?\n\n` +
      `НАХОДКИ:\n${JSON.stringify(found.findings, null, 1)}\n\n` +
      `Верни вердикт по КАЖДОМУ id, ничего не пропуская.`,
      { label: `проверка:${area.key}`, phase: 'Проверка', schema: VERDICT_SCHEMA }
    ).then((v) => ({ area: area.key, label: area.label, found, verdicts: v }))
  }
)

const clean = results.filter(Boolean)
const confirmed = []
const rejected = []
for (const r of clean) {
  const byId = new Map((r.verdicts && r.verdicts.verdicts ? r.verdicts.verdicts : []).map((v) => [v.id, v]))
  for (const f of (r.found && r.found.findings) || []) {
    const v = byId.get(f.id) || null
    const entry = { ...f, severity: (v && v.severity_corrected) || f.severity, area: r.area, area_label: r.label, skeptic: v }
    if (!v || v.real) confirmed.push(entry)
    else rejected.push({ id: f.id, file: f.file, title: f.title, why: (v && v.reason) || '' })
  }
}

const order = { critical: 0, high: 1, medium: 2, low: 3 }
confirmed.sort((a, b) => (order[a.severity] ?? 9) - (order[b.severity] ?? 9))
log(`подтверждено ${confirmed.length}, опровергнуто ${rejected.length}`)

return {
  summaries: clean.map((r) => ({ area: r.area, label: r.label, summary: (r.found && r.found.summary) || '' })),
  confirmed,
  rejected,
}
