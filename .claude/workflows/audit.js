export const meta = {
  name: 'adm-srv-audit',
  description: 'Аудит проекта adm-srv: архитектура, дефекты, дублирование, производительность — с адверсариальной проверкой находок',
  phases: [
    { title: 'Карта', detail: 'архитектурный обзор всего репозитория' },
    { title: 'Поиск', detail: 'находки по восьми направлениям' },
    { title: 'Проверка', detail: 'адверсариальная проверка каждой пачки находок' },
  ],
}

const CONTEXT = `
ПРОЕКТ: /home/user/adm-srv — внутренняя ИТ-платформа Липецкстата (территориальный
орган Росстата). Изолированная сеть, Windows Server 2016, Node.js 22.

Состав:
  helpdesk-backend/  — платформа: заявки, LDAP-вход, прокси модулей, оповещения (~6400 строк)
  CERTVIEWER/        — модуль: реестр сертификатов и МЧД (~1460 строк)
  SMDR/              — модуль: журнал звонков (web-node на Express+EJS) + Collector.Py (~880 строк)
  MESSENGER/         — мессенджер «Искра»

ЖЁСТКИЕ ОГРАНИЧЕНИЯ ПРОЕКТА — предложения, которые их нарушают, бесполезны:
  1. НЕТ шага сборки. Фронтенд — чистые HTML/CSS/JS, никаких бандлеров.
  2. НЕТ новых npm-зависимостей без крайней нужды: сеть изолирована, нативные
     модули (node-gyp) не соберутся. Используется встроенный node:sqlite
     (DatabaseSync — СИНХРОННЫЙ API), встроенный node:test, встроенный zlib.
  3. Браузеры на рабочих машинах — Chromium 109. Никакого oklch, :has(),
     новых API. Веб-шрифтов нет (нет интернета).
  4. MESSENGER/ — ВЕНДОРЕННАЯ КОПИЯ чужого репозитория Lulzdao/Iskra-messenger,
     её периодически сливают с upstream. НЕ предлагай там рефакторинг структуры:
     это создаст вечный конфликт слияния. Можно отмечать только дефекты в наших
     собственных правках и то, что критично для безопасности.
  5. React рассматривали и осознанно отвергли (1300 строк фронта не окупают
     тулчейн на закрытом контуре). Не предлагай фреймворки.
  6. Комментарии в коде на русском и объясняют ПОЧЕМУ, а не ЧТО. Это принятый
     стиль, сохраняй его в предлагаемом коде.
  7. Осознанное ограничение: из архива МЧД сохраняются ТОЛЬКО ФИО, реестровый
     номер и срок. Паспорт/СНИЛС/ИНН не хранятся намеренно — это не недоделка.

Тестов в проекте НЕТ ни одного. Планируется node:test (встроенный).
`

const FINDING_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['area', 'findings'],
  properties: {
    area: { type: 'string' },
    summary: { type: 'string', description: 'Короткая оценка состояния этого участка, 2-3 предложения' },
    findings: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['id', 'title', 'severity', 'category', 'file', 'line', 'now', 'why_bad', 'fix'],
        properties: {
          id: { type: 'string', description: 'краткий слуг, напр. tickets-god-route' },
          title: { type: 'string' },
          severity: { type: 'string', enum: ['critical', 'high', 'medium', 'low'] },
          category: {
            type: 'string',
            enum: ['bug', 'security', 'architecture', 'duplication', 'naming', 'dead-code',
                   'error-handling', 'performance', 'hardcode', 'complexity', 'leaky-abstraction'],
          },
          file: { type: 'string' },
          line: { type: 'integer' },
          now: { type: 'string', description: 'Что сейчас — кратко, с фрагментом кода' },
          why_bad: { type: 'string', description: 'Почему это плохо — конкретное следствие, не «нарушает принципы»' },
          fix: { type: 'string', description: 'Как переписать — НОВЫЙ КОД целиком, готовый к вставке' },
          failure_scenario: { type: 'string', description: 'Для bug/security: конкретные входные данные -> неверный результат. Иначе пустая строка' },
          effort: { type: 'string', enum: ['минуты', 'часы', 'день+'] },
        },
      },
    },
  },
}

const VERDICT_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['verdicts'],
  properties: {
    verdicts: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['id', 'real', 'reason'],
        properties: {
          id: { type: 'string' },
          real: { type: 'boolean', description: 'true — подтверждено чтением кода; false — опровергнуто' },
          severity_corrected: { type: 'string', enum: ['critical', 'high', 'medium', 'low', ''] },
          reason: { type: 'string', description: 'Что именно проверил и что увидел в коде' },
          fix_ok: { type: 'boolean', description: 'Предложенный код корректен и не нарушает ограничений проекта' },
          fix_problem: { type: 'string', description: 'Если fix_ok=false — что не так с предложенным кодом' },
        },
      },
    },
  },
}

phase('Карта')
const map = await agent(`${CONTEXT}

Составь архитектурную карту репозитория /home/user/adm-srv. Прочитай:
  - корневой README.md
  - helpdesk-backend/server.js, config/*, routes/*, services/*, middleware/*, db/*
  - CERTVIEWER/server.js, CERTVIEWER/mchd.js
  - SMDR/web-node/server.js, SMDR/web-node/db.js
  - MESSENGER/server.js (только чтобы понять границу, менять не будем)

Опиши:
1. Как компоненты связаны: кто кого зовёт, где границы процессов, где проходят
   доверительные границы (что проверяет вход, что доверяет вызывающему).
2. Слои внутри helpdesk-backend (config/routes/services/db/public) — соблюдаются
   ли они, где протекают.
3. Где живёт состояние: какие базы, какие файлы, что общее между процессами.
4. Что в архитектуре сделано ХОРОШО и почему это стоит сохранить.
5. Что в архитектуре вызывает вопросы.

Верни текст на русском, плотный, без воды. 600-900 слов.`, { label: 'карта-архитектуры' })

const AREAS = [
  {
    key: 'tickets',
    label: 'заявки',
    prompt: `Разбери helpdesk-backend/routes/tickets.js (579 строк) — это самый большой маршрут проекта.
Также посмотри middleware/auth.js и config/departments.js, они с ним связаны.

Ищи: слишком длинные функции и обработчики, повторяющуюся валидацию, смешение
слоёв (SQL прямо в обработчике), дублирование правил доступа, магические
константы, места где ошибка проглатывается, N+1 запросы, небезопасную работу
с путями при загрузке вложений, неполную валидацию входа.

Обрати особое внимание на функции canAccessTicket, getTicketDetail, nextDisplayId,
обработку вложений (multer, путь на диске) и на то, как считаются права разных ролей.`,
  },
  {
    key: 'frontend',
    label: 'фронтенд платформы',
    prompt: `Разбери helpdesk-backend/public/app.js (1815 строк) и public/styles.css (473 строки).
Это единый файл-модуль без сборки — «god-модуль» по определению, вопрос в том,
где он реально болит.

Ищи: функции длиннее ~80 строк, повторяющиеся куски разметки, ручное
экранирование esc() которое можно забыть (проверь, НЕТ ЛИ МЕСТ ГДЕ ЗАБЫЛИ —
это была бы XSS), полную перерисовку innerHTML там где теряется состояние,
дублирование констант между фронтендом и сервером (статусы, приоритеты, цвета),
мёртвый код, обработчики без снятия (утечки), необработанные отказы fetch.

ВАЖНО: не предлагай React/Preact/бандлеры — это уже обсуждалось и отвергнуто.
Предлагай улучшения в рамках чистого JS без сборки.`,
  },
  {
    key: 'services',
    label: 'сервисы платформы',
    prompt: `Разбери сервисы helpdesk-backend/services/:
  tls.js (366), notifications.js (256), mailer.js (214), scheduler.js (148),
  ldapAuth.js (161), moduleClient.js (57), userStore.js (46), settings.js,
  network.js, sources/certs.js (187), sources/smdr.js (66)

Ищи: god-модули, протекающие абстракции, дублирование, необработанные ошибки,
гонки (планировщик + запросы к одной базе), утечки ресурсов (незакрытые LDAP
соединения, таймеры), неверную обработку крайних значений дат, хардкод.

Особое внимание:
  - tls.js — самый большой сервис, есть ли там god-модуль
  - scheduler.js — что если tick() перекроется сам с собой (долгий обход > часа)
  - notifications.js — emit() синхронно пишет в БД и асинхронно шлёт почту,
    нет ли гонки или потери
  - sources/certs.js — арифметика дат и порогов, границы (0 дней, високосный
    год, часовые пояса)`,
  },
  {
    key: 'routes-auth',
    label: 'маршруты и вход',
    prompt: `Разбери helpdesk-backend/routes/auth.js (169), routes/admin.js (82),
routes/certificates.js (248), routes/modules.js (110), routes/notifications.js (377),
middleware/auth.js, config/config.js (105), server.js (85).

Ищи прежде всего ДЕФЕКТЫ БЕЗОПАСНОСТИ И ЛОГИКИ:
  - проверки прав: везде ли стоят, нет ли маршрута без requireAuth/requireRole
  - подстановка пользовательского ввода в SQL, в пути файлов, в имена ключей settings
  - ограничение попыток входа: обходится ли оно (сброс, гонка, память)
  - что отдаётся наружу в ответах (не утекают ли пароли, хэши, пути, секреты)
  - обработка ошибок: везде ли JSON, нет ли утечки стека
  - CSRF, cookie, сессии
  - загрузка файлов сертификатов: валидация, размер, тип

Для каждой находки категории security обязательно опиши failure_scenario:
конкретный запрос -> конкретное последствие.`,
  },
  {
    key: 'certviewer',
    label: 'Сертвивер',
    prompt: `Разбери модуль CERTVIEWER целиком:
  server.js (~500 строк), mchd.js (~215), public/index.html, public/mchd.html,
  public/app.css, public/login.html

Ищи: длинные функции, дублирование между index.html и mchd.html (две страницы
писались похоже), разбор ZIP и XML своими руками (mchd.js) — там легко
ошибиться: проверь границы буфера, zip-bomb, вредоносные имена файлов,
переполнение, некорректный deflate. Проверь parseCertificate на устойчивость к
кривым сертификатам. Проверь режим BEHIND_GATEWAY: что именно он отключает и
не открывает ли лишнего.

Особое внимание на readZip(): чтение по смещениям из недоверенного файла.
Может ли специально собранный .zip уронить процесс или прочитать не своё?`,
  },
  {
    key: 'smdr',
    label: 'журнал звонков',
    prompt: `Разбери SMDR:
  web-node/server.js (~200), web-node/db.js, web-node/views/*.ejs,
  web-node/public/style.css, web-node/public/select.js, Collector.Py (~250)

Ищи в web-node: SQL-инъекции (строки собираются конкатенацией — проверь
внимательно), отсутствие авторизации (модуль слушает только 127.0.0.1 — но
достаточно ли этого), неограниченные выборки (SELECT без LIMIT на растущей
таблице calls), N+1, агрегацию в JS вместо SQL, XSS в EJS-шаблонах.

Ищи в Collector.Py: обработка обрывов TCP, кодировки, утечка ресурсов,
что происходит при неверном формате строки SMDR, парсинг длительности.
Учти: пользователь осознанно вернул логин/пароль внутрь скрипта вместо .env —
это его решение, не считай это дефектом, но отметь, если пароль попадает в лог.`,
  },
  {
    key: 'cross',
    label: 'сквозные проблемы',
    prompt: `Смотри НА ВЕСЬ проект сразу, ищи то, что видно только поверх файлов:

1. ДУБЛИРОВАНИЕ между компонентами: одинаковый код разбора .env в CERTVIEWER и
   helpdesk-backend? Одинаковые хелперы дат/экранирования в четырёх фронтендах?
   Одинаковые палитры CSS? Одинаковые кастомные select? Найди всё и оцени,
   что реально стоит выносить, а что дублировать дешевле (компоненты — отдельные
   процессы с отдельными package.json, общей библиотеки между ними нет).

2. МЁРТВЫЙ КОД: экспорты которые никто не импортирует, функции которые никто не
   зовёт, CSS-классы которых нет в разметке, зависимости в package.json которые
   не используются, файлы-сироты. Проверяй фактически (grep), не на глаз.

3. ХАРДКОД: порты, пути, имена доменов, адреса, магические числа — что должно
   быть настройкой, а что законно захардкожено.

4. КОНСИСТЕНТНОСТЬ: одни и те же понятия названы по-разному в разных местах?
   Разные стили обработки ошибок? Разные соглашения об именах?

Проверяй утверждения командами (grep/find), не полагайся на впечатление.`,
  },
  {
    key: 'perf',
    label: 'производительность и данные',
    prompt: `Смотри на весь проект с точки зрения производительности, объёмов и схемы данных.

1. СХЕМА: helpdesk-backend/db/schema.sql, CERTVIEWER (CREATE TABLE в server.js),
   SMDR/web-node/db.js. Есть ли нужные индексы под фактические запросы? Есть ли
   лишние? Правильные ли типы и ограничения? Нормализация?

2. ЗАПРОСЫ: найди N+1 (запрос в цикле), выборки без LIMIT, агрегацию в JS там где
   должен агрегировать SQL, повторную подготовку одних и тех же prepared statement
   в горячем пути.

3. СИНХРОННОСТЬ: node:sqlite синхронный. Где длинные синхронные операции блокируют
   цикл событий на пути запроса? Файловые операции fs.*Sync в обработчиках?

4. РОСТ: таблица calls в SMDR растёт вечно. notification_events и
   notification_deliveries тоже. Есть ли план ротации/очистки? Что сломается на
   100 тыс. записей?

5. ФРОНТЕНД: опрос каждые 20 секунд — сколько это запросов, что тянет,
   можно ли дешевле.

Оценивай в реальных масштабах: организация ~100-200 сотрудников, несколько
заявок в день, звонков — тысячи в месяц.`,
  },
]

phase('Поиск')
const results = await pipeline(
  AREAS,
  (area) => agent(
    `${CONTEXT}\n\nТЫ РАЗБИРАЕШЬ УЧАСТОК: ${area.label}\n\n${area.prompt}\n\n` +
    `ПРАВИЛА ОТВЕТА:\n` +
    `- Читай реальный код, не догадывайся. Проверяй утверждения grep-ом.\n` +
    `- Каждая находка должна быть КОНКРЕТНОЙ: файл, строка, что не так, чем это обернётся.\n` +
    `- Поле fix — это ГОТОВЫЙ НОВЫЙ КОД, который можно вставить, с комментариями на русском,\n` +
    `  объясняющими ПОЧЕМУ так, а не что делает строка.\n` +
    `- Не выдумывай проблемы ради количества. Лучше 5 настоящих, чем 20 придуманных.\n` +
    `- Не предлагай то, что нарушает ограничения проекта (см. выше).\n` +
    `- severity=critical только для того, что уже сейчас ломается или пробивает защиту.\n` +
    `- Стиль проекта хороший: подробные комментарии на русском про ПОЧЕМУ. Не предлагай их сокращать.`,
    { label: `поиск:${area.key}`, phase: 'Поиск', schema: FINDING_SCHEMA }
  ),
  (found, area) => {
    if (!found || !found.findings || !found.findings.length) return { area: area.key, found, verdicts: { verdicts: [] } }
    return agent(
      `${CONTEXT}\n\nТЫ — СКЕПТИК. Другой агент разобрал участок «${area.label}» и выдал находки.\n` +
      `Твоя задача — ОПРОВЕРГНУТЬ каждую. По умолчанию считай находку ложной, пока не убедился в обратном,\n` +
      `открыв файл и прочитав код вокруг указанной строки.\n\n` +
      `Проверяй по пунктам:\n` +
      `1. Существует ли описанное в коде вообще? Может, агент выдумал или перепутал файл.\n` +
      `2. Правда ли это приводит к описанному следствию? Может, дальше по коду есть проверка,\n` +
      `   которую он не заметил.\n` +
      `3. Не является ли это ОСОЗНАННЫМ решением проекта? В коде много комментариев,\n` +
      `   объясняющих почему сделано именно так — прочитай их, прежде чем объявлять дефектом.\n` +
      `4. Корректен ли предложенный код fix? Скомпилируется ли, не ломает ли соседнее,\n` +
      `   не нарушает ли ограничения проекта (нет сборки, нет новых зависимостей, Chromium 109).\n` +
      `5. Не завышена ли severity? critical — только то, что ломается прямо сейчас или\n` +
      `   пробивает защиту.\n\n` +
      `НАХОДКИ:\n${JSON.stringify(found.findings, null, 1)}\n\n` +
      `Верни вердикт по КАЖДОМУ id.`,
      { label: `проверка:${area.key}`, phase: 'Проверка', schema: VERDICT_SCHEMA }
    ).then((v) => ({ area: area.key, label: area.label, found, verdicts: v }))
  }
)

const clean = results.filter(Boolean)
const confirmed = []
const rejected = []
for (const r of clean) {
  const byId = new Map((r.verdicts?.verdicts || []).map((v) => [v.id, v]))
  for (const f of r.found?.findings || []) {
    const v = byId.get(f.id)
    const entry = { ...f, area: r.area, area_label: r.label, verdict: v || null }
    if (!v || v.real) confirmed.push(entry)
    else rejected.push(entry)
  }
}

log(`подтверждено находок: ${confirmed.length}, опровергнуто: ${rejected.length}`)

return {
  map,
  summaries: clean.map((r) => ({ area: r.area, summary: r.found?.summary || '' })),
  confirmed,
  rejected: rejected.map((r) => ({ id: r.id, title: r.title, reason: r.verdict?.reason })),
}
