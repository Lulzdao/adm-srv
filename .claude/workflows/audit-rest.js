export const meta = {
  name: 'adm-srv-audit-rest',
  description: 'Оставшиеся пять направлений аудита adm-srv: сквозные проблемы, производительность, качество тестов, эксплуатация, «Искра»',
  phases: [
    { title: 'Поиск', detail: 'пять направлений, не пройденных прошлыми заходами' },
    { title: 'Проверка', detail: 'скептик опровергает находки своего направления' },
    { title: 'Экспертиза', detail: 'три независимых взгляда на каждую серьёзную находку' },
    { title: 'Полнота', detail: 'что осталось непосмотренным' },
  ],
}

const CONTEXT = `
ПРОЕКТ: /home/user/adm-srv — внутренняя ИТ-платформа Липецкстата (территориальный
орган Росстата). Изолированная сеть без интернета, Windows Server 2016, Node.js 22.
Организация ~100-200 сотрудников, несколько заявок в день, тысячи звонков в месяц.

Состав:
  helpdesk-backend/  — платформа: заявки, вход через LDAP, прокси модулей, оповещения.
                       app.js (сборка express), server.js (запуск), routes/ (7 файлов),
                       services/ (11 файлов), config/, db/, public/app.js (2050 строк)
  CERTVIEWER/        — модуль: реестр сертификатов и МЧД (server.js ~480, mchd.js ~250)
  SMDR/              — модуль: журнал звонков (web-node/server.js 197, db.js ~55, Collector.Py ~290)
  MESSENGER/         — мессенджер «Искра»: server.js 2318, public/index.html 1744, Electron-клиент

ЖЁСТКИЕ ОГРАНИЧЕНИЯ — предложения, которые их нарушают, бесполезны:
  1. НЕТ шага сборки. Фронтенд — чистые HTML/CSS/JS. React рассматривали и отвергли.
  2. НЕТ новых npm-зависимостей без крайней нужды: сеть изолирована, node-gyp не соберётся.
     Поэтому node:sqlite (DatabaseSync — СИНХРОННЫЙ), node:test, встроенный zlib, разбор ZIP руками.
     Playwright и прочие браузерные инструменты на контуре недоступны — сквозных тестов в npm test нет
     и быть не должно.
  3. Браузеры — Chromium 109. Никакого oklch, :has(), веб-шрифтов.
  4. MESSENGER/ — ВЕНДОРЕННАЯ КОПИЯ Lulzdao/Iskra-messenger, её сливают с upstream.
     Структурный рефакторинг там НЕ предлагать: создаст вечный конфликт слияния.
  5. Комментарии на русском объясняют ПОЧЕМУ. Сохраняй этот стиль в предлагаемом коде.
  6. Осознанные решения, НЕ являющиеся недоделками:
     - из архива МЧД хранятся ТОЛЬКО ФИО, реестровый номер и срок (паспорт/СНИЛС/ИНН — намеренно нет);
     - модули слушают 127.0.0.1 и доверяют запросам за платформой (CERTVIEWER при BEHIND_GATEWAY=true);
     - в тестах не генерируются сертификаты через openssl: тесты должны идти на боевом Windows.

ЧТО УЖЕ НАЙДЕНО И ИСПРАВЛЕНО В ЭТОМ ЖЕ АУДИТЕ — ПОВТОРНО НЕ СООБЩАТЬ:
  - getTicketDetail отдавал внутренние заметки (is_internal) заявителю — закрыто;
  - esc() в public/app.js не экранировал кавычки (XSS в атрибутах) — закрыто;
  - ограничение попыток входа обходилось пачкой одновременных запросов — закрыто (claimAttempt);
  - nextDisplayId считал через COUNT(*) при UNIQUE display_id — закрыто;
  - tick() планировщика не был защищён от наложения — закрыто (флаг ticking);
  - deliverPending и retryPending слали письмо дважды — закрыто (общая очередь enqueueMail);
  - inspectTlsOptions не имел предела ожидания — закрыто (5 с);
  - имена вложений искажались (latin1 вместо utf8) — закрыто (defParamCharset) в платформе и Сертвивере;
  - отказ вложения плодил вторую заявку — закрыто (создание и прикрепление разделены);
  - readZip распаковывал архив без пределов (zip-бомба) — закрыто (MAX_ENTRIES/MAX_ENTRY_BYTES/MAX_TOTAL_BYTES);
  - охрана страниц Сертвивера обходилась через /%69ndex.html — закрыто (проверка раскодированного пути);
  - SMDR: ошибка базы рвала сессию с АТС, не было WAL и индексов — закрыто (save_call, WAL, 3 индекса);
  - fs.watch без обработчика 'error' ронял платформу — закрыто;
  - панель врала, что «Искра» подхватит сертификат сама — текст исправлен;
  - server.js разделён на app.js (сборка) и server.js (запуск) — это и позволило тестировать маршруты.

ТЕСТЫ: 119 настоящих, все зелёные. Запуск — npm test (шаблон test/**/*.test.js).
  helpdesk-backend: 93 — notifications 17, expiry 16, scheduler 15, tickets 12, tls 11,
                    localAccounts 7, frontend 6, proxy 5, auth 4
  CERTVIEWER: 26 — mchd.test.js
  Оснастка: test/helpers/{tempDb,fakeSmtp,fakeModules,httpApp}.js, CERTVIEWER/test/zipFixture.js
ВАЖНО: раньше "node --test" без аргумента считал файлы оснастки за пройденные тесты — счёт врал.

ЗАПРЕТ НА ДАННЫЕ: в проекте есть архивы с персональными данными. НИКОГДА не приводи
настоящие ФИО, СНИЛС, ИНН, паспорта, реестровые номера, адреса почты сотрудников.
Все примеры — выдуманные. Файлы helpdesk-backend/data/*.db и SMDR/*.db не читай.
`

const FINDING_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['area', 'summary', 'findings'],
  properties: {
    area: { type: 'string' },
    summary: { type: 'string', description: 'Честная оценка направления, 3-5 предложений: что хорошо, что болит' },
    findings: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['id', 'title', 'severity', 'category', 'file', 'line', 'now', 'why_bad', 'fix', 'failure_scenario', 'effort', 'evidence'],
        properties: {
          id: { type: 'string', description: 'краткий слуг, напр. cross-dead-export' },
          title: { type: 'string' },
          severity: { type: 'string', enum: ['critical', 'high', 'medium', 'low'] },
          category: {
            type: 'string',
            enum: ['bug', 'security', 'architecture', 'duplication', 'naming', 'dead-code',
                   'error-handling', 'performance', 'hardcode', 'complexity', 'leaky-abstraction',
                   'test-gap', 'false-confidence', 'ops'],
          },
          file: { type: 'string' },
          line: { type: 'integer' },
          now: { type: 'string', description: 'Что сейчас — кратко, с точным фрагментом кода' },
          why_bad: { type: 'string', description: 'Конкретное следствие, не «нарушает принципы»' },
          fix: { type: 'string', description: 'ГОТОВЫЙ НОВЫЙ КОД, с русскими комментариями про ПОЧЕМУ' },
          failure_scenario: { type: 'string', description: 'Конкретные входные данные -> неверный результат. Пустая строка, если неприменимо' },
          evidence: { type: 'string', description: 'ЧЕМ проверено: команда grep/запуск/замер и ЧТО она показала. «прочитал код» — недостаточно для bug/security/performance' },
          effort: { type: 'string', enum: ['минуты', 'часы', 'день+'] },
        },
      },
    },
  },
}

const VERDICT_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['verdicts'],
  properties: {
    verdicts: {
      type: 'array',
      items: {
        type: 'object',
        additionalProperties: false,
        required: ['id', 'real', 'reason', 'fix_ok'],
        properties: {
          id: { type: 'string' },
          real: { type: 'boolean' },
          severity_corrected: { type: 'string', enum: ['critical', 'high', 'medium', 'low', ''] },
          reason: { type: 'string', description: 'Что именно проверил и что увидел в коде' },
          fix_ok: { type: 'boolean' },
          fix_problem: { type: 'string' },
        },
      },
    },
  },
}

const LENS_SCHEMA = {
  type: 'object',
  additionalProperties: false,
  required: ['refuted', 'note'],
  properties: {
    refuted: { type: 'boolean', description: 'true — находка НЕ подтвердилась' },
    note: { type: 'string', description: 'Что проверил и что увидел, со ссылкой на файл:строку' },
    severity_corrected: { type: 'string', enum: ['critical', 'high', 'medium', 'low', ''] },
    fix_ok: { type: 'boolean' },
  },
}

// Порядок важен: если прогон оборвётся на пределе сессии, доделанными окажутся
// первые направления. Впереди то, где вероятнее найти настоящий вред.
const AREAS = [
  {
    key: 'perf',
    label: 'производительность платформы и схема данных',
    prompt: `Направление: производительность, объёмы и схема данных В ПЛАТФОРМЕ (helpdesk-backend).
Журнал звонков уже разобран отдельно — его не трогай.

1. СХЕМА: db/schema.sql целиком. Найди grep-ом ВСЕ запросы в routes/ и services/ и сопоставь
   с индексами: под какие запросы индекса нет, какие индексы никем не используются.
   Проверь ОТДЕЛЬНО: включён ли PRAGMA foreign_keys (в SQLite он по умолчанию ВЫКЛЮЧЕН —
   посмотри db/init.js и убедись сам, а не по комментарию); включён ли WAL; какие ON DELETE
   заданы и что произойдёт при удалении пользователя, отдела, заявки.
   Проверь утверждения запуском: подними временную базу через test/helpers/tempDb.js,
   налей в неё выдуманных данных и посмотри EXPLAIN QUERY PLAN.

2. ЗАПРОСЫ: N+1 (запрос в цикле), выборки без LIMIT, агрегация в JS вместо SQL,
   повторная подготовка одного prepared statement в горячем пути, COUNT(*) на каждой отрисовке.
   GET /api/tickets отдаёт всю таблицу — посчитай, что это значит на 5000 заявок.

3. СИНХРОННОСТЬ: node:sqlite синхронный. Где длинные синхронные операции блокируют цикл
   событий на пути запроса? fs.*Sync в обработчиках? bcrypt? отправка почты внутри запроса?
   Замерь то, что можно замерить.

4. РОСТ: notification_events, notification_deliveries, tickets, uploads/ растут вечно.
   Есть ли ротация? Посчитай объём за год при реальных масштабах и скажи, что сломается первым.

5. ФРОНТЕНД: public/app.js — опрос по таймеру. Найди ВСЕ setInterval/setTimeout-опросы,
   посчитай запросы в минуту с одного рабочего места, посмотри что тянет каждый и сколько
   весит ответ. Кэширование статики, ETag, размер отдаваемых файлов.

Каждое утверждение про скорость и объём подкрепляй ЗАМЕРОМ или расчётом с числами.`,
  },
  {
    key: 'tests',
    label: 'качество существующих тестов',
    prompt: `Направление: НЕ код, а ТЕСТЫ. helpdesk-backend/test/*.test.js (93 теста),
test/helpers/*.js, CERTVIEWER/test/mchd.test.js (26), CERTVIEWER/test/zipFixture.js.

Ищи ЛОЖНУЮ УВЕРЕННОСТЬ — тесты, которые зелены, но ничего не доказывают:
  - проверяют то, что не может сломаться (константы, геттеры, возвращаемое значение,
    которое undefined в любом случае);
  - без assert на главном свойстве (проверяют, что не упало, а не что сделано верно);
  - моки, повторяющие реализацию вместо её проверки;
  - общее состояние между тестами, зависимость от порядка выполнения;
  - висящие ресурсы: сервер, таймер, база не закрыты — тест проходит, процесс живёт;
  - недетерминированность, гонки, ожидания через setTimeout вместо условия.

ОБЯЗАТЕЛЬНО ПРОВЕРЬ ЗАПУСКОМ, а не рассуждением:
  а) МУТАЦИИ. Для 8-10 самых важных проверок внеси ломающую правку в КОД (не в тест),
     запусти npm test, запиши, упал ли тест. Обязательно верни файл: git checkout -- <файл>,
     и убедись, что npm test снова зелёный. В поле evidence пиши результат опыта:
     «сломал X в файле Y — не заметил ни один тест» или «сломал X — упало 3 теста».
     Находки категории false-confidence должны опираться ИМЕННО на это.
  б) ЧАСОВЫЕ ПОЯСА. Запусти в обоих каталогах:
       TZ=Pacific/Kiritimati npm test     (UTC+14)
       TZ=Pacific/Niue npm test           (UTC-11)
     Если что-то падает — это находка, и серьёзная: «в пятницу упало» начинается отсюда.
  в) ПОРЯДОК И ПОВТОРЫ. Запусти npm test дважды подряд, потом с --test-concurrency=1.
     Сравни результат.
  г) ВИСЯЩИЕ РЕСУРСЫ. Проверь, завершается ли процесс сам: время выполнения npm test,
     не держит ли что-то цикл событий.

Отдельно перечисли, ЧТО НЕ ПОКРЫТО и является критическим путём (категория test-gap).
Замерь покрытие: node --test --experimental-test-coverage "test/**/*.test.js".

В КОНЦЕ ОБЯЗАТЕЛЬНО: git status должен быть чистым, npm test зелёным в обоих каталогах.
Если оставил правку — верни её. Это не пожелание: рабочее дерево чужое.`,
  },
  {
    key: 'cross',
    label: 'сквозные проблемы',
    prompt: `Направление: то, что видно только поверх файлов, во всём репозитории сразу.
Проверяй утверждения командами (grep/find/wc), НЕ на глаз. В evidence — сама команда и её вывод.

1. ДУБЛИРОВАНИЕ между компонентами. Найди и сопоставь ФАКТИЧЕСКИ:
   - функции экранирования (esc/escapeHtml) в четырёх фронтендах — сколько копий, разошлись ли;
   - форматирование дат;
   - кастомные выпадающие списки (их точно несколько — сравни реализации);
   - палитры CSS и наборы переменных;
   - разбор .env;
   - проверку сессии.
   Для КАЖДОГО дубля честно оцени: выносить или дублировать дешевле. Компоненты — отдельные
   процессы с отдельными package.json, общей библиотеки нет, и заводить её ради трёх функций
   может быть хуже дублирования. Ответ «дублировать дешевле» — нормальный ответ.
   ОСОБО ВАЖНО: если копии УЖЕ РАЗОШЛИСЬ (в одной есть исправление, в другой нет) — это дефект,
   а не стилистика. Проверь это буквально, сравнив тексты функций.

2. МЁРТВЫЙ КОД. Проверяй grep-ом, а не впечатлением:
   - экспорты, которые никто не импортирует (пройди по module.exports каждого файла);
   - функции, которые никто не зовёт;
   - CSS-классы, которых нет ни в одной разметке и ни в одном JS;
   - зависимости package.json, не используемые в коде — проверь КАЖДУЮ по имени;
   - файлы-сироты, закомментированные куски, оставшиеся отладочные console.log.

3. ХАРДКОД: порты, пути, доменные имена, адреса почты, магические числа.
   Раздели: что законно захардкожено (адрес аварийной учётки), а что должно быть настройкой.

4. КОНСИСТЕНТНОСТЬ: одни понятия названы по-разному в разных местах? Разный формат ответов API
   (где-то {error}, где-то {ok:false})? Разные соглашения об именах? Разные стили обработки ошибок
   в соседних файлах?

5. ДОКУМЕНТАЦИЯ И ВЫКЛАДКА: README.md в корне и в каждом компоненте, .env.example.
   Сопоставь ФАКТИЧЕСКИ: выпиши все process.env.* из кода и все переменные из .env.example,
   покажи расхождения в обе стороны. Верны ли инструкции запуска? Не устарели ли описания
   после недавних правок (app.js/server.js, npm test по шаблону, WAL в SMDR)?

6. WINDOWS: проект живёт на Windows Server 2016. Ищи места, где путь собирается через "/",
   где важен регистр имён файлов, где кодировка консоли ломает вывод, где fs.watch ведёт себя
   иначе, где перевод строк CRLF имеет значение.`,
  },
  {
    key: 'ops',
    label: 'эксплуатация и восстановление',
    prompt: `Направление: проект глазами того, кто держит его на одном Windows-сервере без ИТ-отдела.

1. ЗАПУСК И ПАДЕНИЕ. Что произойдёт при старте каждого компонента, если: база повреждена,
   каталог uploads недоступен, порт занят, .env отсутствует, домен недоступен, диск полон?
   Процесс падает с понятным сообщением или сыпет стеком? ПРОВЕРЬ ЗАПУСКОМ хотя бы часть:
   подними платформу с занятым портом, с битым файлом базы, без .env — и покажи, что видно в консоли.
   Есть ли обработчики process.on('uncaughtException') и ('unhandledRejection')? Если нет —
   что случится при необработанном отказе промиса в Node 22 (процесс падает по умолчанию).

2. МИГРАЦИИ СХЕМЫ. Как накатывается изменение schema.sql на РАБОТАЮЩУЮ базу с данными?
   Посмотри db/init.js и migrateNotifications: идемпотентны ли, что при частичном применении,
   есть ли версия схемы. Что произойдёт при обновлении версии на боевом сервере — данные переживут?
   ПРОВЕРЬ: возьми базу, созданную прежней схемой (сделай её руками), примени init.js и посмотри.

3. РЕЗЕРВНОЕ КОПИРОВАНИЕ. Можно ли скопировать .db на живой системе? Учти WAL: копия одного
   файла без -wal и -shm неполна. Описано ли это где-нибудь? Что именно надо копировать
   (базы платформы, Сертвивера, журнала звонков, «Искры», uploads/, certs/)?

4. ЛОГИ. Куда пишет каждый компонент, ротируются ли, попадают ли туда пароли, персональные
   данные, содержимое писем? Можно ли по логу понять, что случилось у конкретного пользователя?
   Проверь grep-ом, не логируется ли пароль или закрытый ключ.

5. ДИАГНОСТИКА. Пользователь говорит «письмо не пришло» — чем это посмотреть? «Модуль не
   открывается» — видно ли, почему? «Не могу войти» — есть ли что-то, кроме догадок?
   Оцени, чего не хватает, и предложи минимальное дополнение.

6. ОБНОВЛЕНИЕ. Описан ли порядок выкладки? Останавливаются ли службы? Что с открытыми сессиями
   (они в памяти процесса)? Что с незавершёнными доставками писем?

Читай README.md в корне и в каждом компоненте, .env.example, db/init.js, server.js каждого
компонента, обработчики ошибок. Находки — конкретные, с файлом и строкой.`,
  },
  {
    key: 'messenger',
    label: '«Искра»: безопасность и наши правки',
    prompt: `MESSENGER/ — вендоренная копия чужого репозитория Lulzdao/Iskra-messenger, её сливают
с upstream. СТРУКТУРНЫЙ РЕФАКТОРИНГ ПРЕДЛАГАТЬ НЕЛЬЗЯ.

Смотри ровно на две вещи.

1. ДЕФЕКТЫ БЕЗОПАСНОСТИ в MESSENGER/server.js (2318) и public/index.html (1744):
   - JWT: какой алгоритм, проверяется ли он явно (уязвимость alg=none / подмена на HS256),
     откуда берётся секрет, есть ли значение по умолчанию, какой срок жизни, есть ли отзыв;
   - права администратора: пройди по КАЖДОМУ маршруту /api/admin/* и проверь middleware;
   - SQL: собираются ли запросы конкатенацией (проверь каждый);
   - XSS: подстановки в innerHTML в public/index.html — везде ли экранирование;
   - загрузка файлов: тип, размер, имя, куда кладётся, отдаётся ли обратно;
   - РАЗДАЧА ОБНОВЛЕНИЙ КЛИЕНТА (MESSENGER/updates/): кто может туда положить файл и кто
     может его скачать. Это канал доставки исполняемого кода на рабочие машины — если туда
     можно записать без прав администратора, это самое опасное, что есть в проекте;
   - хранение паролей (bcryptjs — какая стоимость), сброс пароля, стартовый администратор;
   - WebSocket: как аутентифицируется соединение, можно ли слушать чужие сообщения,
     проверяется ли право на комнату при каждом сообщении или только при подключении.

2. НАШИ СОБСТВЕННЫЕ ПРАВКИ. Найди их: git log --oneline -- MESSENGER/ и git blame по файлам.
   Это presenceSnapshot() и /api/admin/presence в server.js; разметка и CSS списка пользователей
   в public/index.html (.user-row, .dept-block, перетаскивание отделов, группировка по отделам,
   applyDeptOrder, renderUsers, userRowHtml). Здесь ищи обычные дефекты: гонки, утечки
   обработчиков при перерисовке, неверную сортировку, доступность с клавиатуры, поведение при
   пустом списке, откат при отказе сервера, потерю позиции прокрутки.

Каждую находку помечай в title префиксом «[наше]» или «[upstream]». Для [upstream] находок
severity ставь по фактической опасности, но в fix пиши правку, которая переживёт слияние:
точечную, без перестановки кода.`,
  },
]

const LENSES = [
  {
    key: 'воспроизведение',
    ask: `Ты проверяешь ВОСПРОИЗВОДИМОСТЬ. Составь конкретный сценарий: какие входные данные,\n` +
         `какая последовательность действий, какой неверный результат. Пройди по коду руками.\n` +
         `Где можешь — проверь запуском или grep-ом, а не рассуждением.\n` +
         `Если связный сценарий не выходит — находка не подтверждена, refuted=true.`,
  },
  {
    key: 'осознанность',
    ask: `Ты проверяешь ОСОЗНАННОСТЬ. Прочитай комментарии вокруг этого места, README компонента,\n` +
         `историю правок (git log -p по файлу). В этом проекте много осознанных решений, и причина\n` +
         `обычно записана прямо в коде. Если это решение, а не дефект — refuted=true.`,
  },
  {
    key: 'правка',
    ask: `Ты проверяешь ПРЕДЛОЖЕННЫЙ КОД. Мысленно примени его: выполнится ли, не сломает ли\n` +
         `соседнее, не разойдётся ли с вызывающими, не нарушает ли ограничений проекта,\n` +
         `не уронит ли существующие 119 тестов. Заполни fix_ok и объясни.\n` +
         `refuted=true ставь ТОЛЬКО если, разбирая правку, ты понял, что исходной проблемы не было.`,
  },
]

const MAX_HEAVY_PER_AREA = 6

phase('Поиск')
const results = await pipeline(
  AREAS,
  // 1. Поиск
  (area) => agent(
    `${CONTEXT}\n\nТЫ РАЗБИРАЕШЬ НАПРАВЛЕНИЕ: ${area.label}\n\n${area.prompt}\n\n` +
    `ПРАВИЛА ОТВЕТА:\n` +
    `- Читай реальный код целиком, не догадывайся. Проверяй утверждения командами и запуском.\n` +
    `- Поле evidence обязательно: чем именно проверено и что показало. Для bug/security/performance\n` +
    `  «прочитал код» недостаточно — нужна команда, замер или воспроизведение.\n` +
    `- Каждая находка КОНКРЕТНА: файл, строка, что не так, чем обернётся.\n` +
    `- Поле fix — ГОТОВЫЙ НОВЫЙ КОД, который можно вставить, с русскими комментариями про ПОЧЕМУ.\n` +
    `- Не выдумывай проблемы ради количества. Пять настоящих лучше двадцати придуманных.\n` +
    `- Прежде чем объявить дефектом — прочитай комментарии вокруг: осознанных решений тут много.\n` +
    `- severity=critical только для того, что уже сейчас ломается или пробивает защиту.\n` +
    `- Файлы проекта НЕ МЕНЯЙ. Единственное исключение — опыты с мутациями в задании про тесты,\n` +
    `  и там всё обязано вернуться назад (git checkout), а рабочее дерево — остаться чистым.`,
    { label: `поиск:${area.key}`, phase: 'Поиск', schema: FINDING_SCHEMA }
  ),
  // 2. Скептик того же направления
  (found, area) => {
    if (!found || !found.findings || !found.findings.length) {
      return { area: area.key, label: area.label, found, verdicts: { verdicts: [] } }
    }
    return agent(
      `${CONTEXT}\n\nТЫ — СКЕПТИК. Другой агент разобрал направление «${area.label}» и выдал находки.\n` +
      `Твоя задача — ОПРОВЕРГНУТЬ каждую. По умолчанию находка ложная, пока ты не убедился\n` +
      `в обратном, открыв файл и прочитав код вокруг указанной строки.\n\n` +
      `1. Существует ли описанное в коде вообще? Может, агент выдумал строку или перепутал файл.\n` +
      `2. Правда ли это приводит к описанному следствию? Может, дальше есть проверка,\n` +
      `   которую он не заметил, или вызывающий уже всё делает сам.\n` +
      `3. Не осознанное ли это решение проекта? Прочитай комментарии рядом.\n` +
      `4. ПРОВЕРЬ ПОЛЕ evidence: если там «прочитал код» вместо команды или замера — усомнись\n` +
      `   особенно сильно и проверь сам.\n` +
      `5. Корректен ли предложенный код? Не уронит ли существующие 119 тестов?\n` +
      `6. Не завышена ли severity? critical — только то, что ломается прямо сейчас или пробивает защиту.\n\n` +
      `НАХОДКИ:\n${JSON.stringify(found.findings, null, 1)}\n\n` +
      `Верни вердикт по КАЖДОМУ id, ничего не пропуская.`,
      { label: `проверка:${area.key}`, phase: 'Проверка', schema: VERDICT_SCHEMA }
    ).then((v) => ({ area: area.key, label: area.label, found, verdicts: v }))
  },
  // 3. Тройная экспертиза серьёзных находок ЭТОГО направления.
  //    Стоит внутри конвейера, а не после барьера: если прогон оборвётся на
  //    пределе сессии, доделанные направления окажутся проверенными целиком.
  async (r) => {
    if (!r || !r.found) return r
    const byId = new Map((r.verdicts && r.verdicts.verdicts ? r.verdicts.verdicts : []).map((v) => [v.id, v]))
    const survived = []
    const rejected = []
    for (const f of r.found.findings || []) {
      const v = byId.get(f.id) || null
      const entry = { ...f, severity: (v && v.severity_corrected) || f.severity, area: r.area, area_label: r.label, skeptic: v }
      if (!v || v.real) survived.push(entry)
      else rejected.push({ id: f.id, file: f.file, title: f.title, why: (v && v.reason) || '' })
    }

    const heavyAll = survived.filter((f) => f.severity === 'critical' || f.severity === 'high')
    const heavy = heavyAll.slice(0, MAX_HEAVY_PER_AREA)
    if (heavyAll.length > heavy.length) {
      log(`${r.label}: на тройную экспертизу взяты ${heavy.length} находок из ${heavyAll.length} — остальные идут с одним вердиктом`)
    }
    const light = survived.filter((f) => !heavy.includes(f))

    const judged = await parallel(heavy.map((f) => () =>
      parallel(LENSES.map((lens) => () => agent(
        `${CONTEXT}\n\nТЫ — НЕЗАВИСИМЫЙ ЭКСПЕРТ. Проверяешь ОДНУ находку из аудита.\n${lens.ask}\n\n` +
        `НАХОДКА:\n${JSON.stringify({
          title: f.title, severity: f.severity, category: f.category, file: f.file, line: f.line,
          now: f.now, why_bad: f.why_bad, failure_scenario: f.failure_scenario,
          evidence: f.evidence, fix: f.fix,
        }, null, 1)}\n\n` +
        `Открой файл ${f.file} и прочитай код вокруг строки ${f.line}. Не верь описанию на слово.`,
        { label: `экспертиза:${f.id}:${lens.key}`, phase: 'Экспертиза', schema: LENS_SCHEMA }
      ))).then((votes) => {
        const good = votes.filter(Boolean)
        const against = good.filter((v) => v.refuted).length
        const corrected = good.map((v) => v.severity_corrected).filter(Boolean)
        const rank = ['critical', 'high', 'medium', 'low']
        return {
          ...f,
          lenses: good,
          // Один несогласный из трёх — нормальный разброс, а не приговор находке.
          confirmed: good.length ? against < Math.ceil(good.length / 2) : true,
          severity: corrected.length
            ? corrected.slice().sort((a, b) => rank.indexOf(a) - rank.indexOf(b))[Math.floor(corrected.length / 2)]
            : f.severity,
          fix_ok: good.every((v) => v.fix_ok !== false),
        }
      })
    ))

    const ok = judged.filter(Boolean).filter((f) => f.confirmed)
    const out = judged.filter(Boolean).filter((f) => !f.confirmed)
    return {
      area: r.area,
      label: r.label,
      summary: (r.found && r.found.summary) || '',
      confirmed: [...ok, ...light],
      rejected: [...rejected, ...out.map((f) => ({
        id: f.id, file: f.file, title: f.title,
        why: f.lenses.filter((l) => l.refuted).map((l) => l.note).join(' | '),
      }))],
    }
  }
)

const clean = results.filter(Boolean)
const confirmed = clean.flatMap((r) => r.confirmed || [])
const rejected = clean.flatMap((r) => r.rejected || [])
const rank = { critical: 0, high: 1, medium: 2, low: 3 }
confirmed.sort((a, b) => (rank[a.severity] ?? 9) - (rank[b.severity] ?? 9))
log(`итого подтверждено ${confirmed.length}, опровергнуто ${rejected.length}`)

phase('Полнота')
const gaps = await agent(
  `${CONTEXT}\n\nТы — КРИТИК ПОЛНОТЫ. Аудит проекта закончен: прошлые заходы разобрали заявки,\n` +
  `фронтенд платформы, сервисы, маршруты и вход, Сертвивер, журнал звонков, TLS; этот заход —\n` +
  `${AREAS.map((a) => a.label).join(', ')}.\n\n` +
  `Оценки направлений этого захода:\n${clean.map((r) => `- ${r.label}: ${r.summary || '—'}`).join('\n')}\n\n` +
  `Подтверждённые находки этого захода (только заголовки):\n` +
  `${confirmed.map((f) => `- [${f.severity}] ${f.file}: ${f.title}`).join('\n') || '(нет)'}\n\n` +
  `Пройди по репозиторию сам (find/grep) и ответь:\n` +
  `1. Какие ФАЙЛЫ не попали ни в одно направление ни разу? Перечисли и скажи, стоит ли их смотреть.\n` +
  `2. Какие КЛАССЫ проблем не искали вовсе? Например: доступность интерфейса, поведение при\n` +
  `   отключении JavaScript, печать, работа с клавиатуры, локализация, права на файлы,\n` +
  `   поведение при одновременной работе двух администраторов, лицензии зависимостей.\n` +
  `3. Какие утверждения аудита выглядят непроверенными и стоит перепроверить в первую очередь?\n` +
  `Отвечай конкретно, со ссылками на файлы. 400-700 слов.`,
  { label: 'полнота', phase: 'Полнота' }
)

return {
  summaries: clean.map((r) => ({ area: r.area, label: r.label, summary: r.summary })),
  confirmed,
  rejected,
  gaps,
  counts: { подтверждено: confirmed.length, опровергнуто: rejected.length },
}
